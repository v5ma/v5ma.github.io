# Theology Wiki: source-based research edition

The live collection retains its established SAN-style reader at `san-reader.html`; `index.html` remains compatible. This edition develops the actual published discussions instead of replacing them with an unrelated reference encyclopedia.

The current collection indexes all 354 already-public AI chats and contains 22 developed articles. Its investigations cover First Beast/Trump, the Ukraine/Russia forecast record, El in Egypt, Jesus as the Teacher of Righteousness, Moses/volcano chronology, the Kenite hypothesis, and the earlier articles on Apocalyptic Repair Theology, Cognitive Gnosticism, Christ as an inner model, Antichrist as a pattern of conduct, religion for conscious robots, deliberate joy, the Temple-trauma hypothesis, and Samaritan texts and sacred authority. Eight topic collections, seven reading paths, explicit backlinks, source links and a focused graph connect the material. The rest of the archive remains conversation records and legacy notes, not 354 newly reviewed essays. Topic assignments are provisional.

The reader supports full-discussion keyword search, title/date/backlink sorting, source-only and article-only filters, bookmark export/restore, a paginated conversation catalogue, top-level speaker labels, search within each conversation, and links to individual turns. A user turn may contain pasted AI text; the display preserves that fact rather than inventing a new speaker. Opening a conversation checks its bytes against the recorded SHA-256 manifest when the browser supports Web Crypto. No private-repository content is copied into this edition.

Three museum pictures are bundled locally after the Metropolitan Museum of Art API identifies each as public domain. Captions retain institutional links, creator/date, collection credit and interpretive context. `data/media.json` is the machine-readable provenance record. Pictures are not evidence that an author's historical, theological or scientific proposal has been established.

## Source and build

Run `node theology-wiki/tools/build.cjs` from the repository root, then `node --test theology-wiki/tests/research.test.cjs`. No Node dependencies are needed. The build verifies the complete existing source manifest before generating the page index, search index, graph, new articles and local reader adapter. It never modifies the original chat files. Editorial source is `editorial/edition.cjs` and the additive `editorial/expansion.cjs`; generated article Markdown is in `content/developed/`.

The shared SAN runtime is read, not modified. A Theology-local adapter is generated at `assets/js/theology-reader.js`. The existing interactive-project integration script receives a narrow guard so it does not overwrite the improved Theology entry point on a future game build. The project homepage, games and shared SAN shell remain outside the edit scope.

`python theology-wiki/tools/media.py` downloads and verifies the three pictures; it requires Pillow and network access. Subsequent builds verify already-bundled image hashes and do not redownload them. The dependency versions used by CI are recorded in the workflow.

Serve the repository root with `python -m http.server 4174 --bind 127.0.0.1`. Open `http://127.0.0.1:4174/theology-wiki/san-reader.html`. The reader fetches JSON and Markdown, so a local HTTP server is required; directly opening a local HTML file is not the supported path.

## Validation

`node --test theology-wiki/tests/research.test.cjs` verifies source integrity, routes, graph destinations, full-text indexing, parsing, filters, export validation and the integration guard. `python theology-wiki/tests/browser.py` uses native Chromium over HTTP; it needs Python Playwright and its Chromium install. The suite tests real source requests, locally served images and real-origin bookmark persistence, and saves screenshots and a JSON report under `theology-test-output/`.

The final GitHub Actions workflow is read-only: it verifies a deterministic rebuild, runs native browser tests before publication, and compares hosted assets and repeats the browser suite after publication. Its artifacts are the test evidence. A successful build is not a claim that every interpretation in every archived conversation has been independently fact-checked. New articles preserve authorial reasoning and corrections while identifying historical hypotheses and AI-generated additions.

## Preservation and rollback

All original chat files and existing source-note Markdown are retained. The existing home and source-index landing pages are reorganized; old routes remain available. Git history is the reversible backup. The old generic private PR is separate from this public source-based edition.

Publishing means merging only the reviewed Theology changes and the scoped integration guard into the public repository's master branch. Never publish the private parent repository or local private archives. The public application has no accounts, analytics, external scripts, paid APIs or runtime AI calls.

## Source expansion and dated records

The second source edition adds six developed investigations, bringing the collection to 14 developed articles and 397 indexed routes. Each new article has an explicit claim-type label, archived background or argument references, and external sources with honest access notes. A restricted abstract is not described as a full-text review. The original eight article bodies remain in their editorial module; several generated pages gain reciprocal links to the new investigations.

`data/forecast-ledger.json` has six dated records: two archived-chat records, two publication records and two recovery leads. An export-start date is not assigned to every later turn. In particular, the October 12, 2025 update in the March-started chat retains its in-turn date. The July/August 2026 fuel-and-famine items are explicitly paraphrased context leads, not new exported conversations or independently verified timestamps. No outcome audit has been completed and no fulfilled-prediction count is presented. Proposed evaluation criteria are editorial, not claimed original preregistrations.

The forecast explorer supports text/type filters, chronological sorting, JSON export and direct links to exact author turns. The generated Markdown retains the complete register as a noninteractive counterpart. `data/external-sources.json` documents 15 research/publication references and their access scopes. Alias indexing makes the Thera/Ahmose discussion discoverable despite its original Parthians and Medes title. Source hashes and original source-note files remain unchanged.

Cache versions are refreshed on both reader entry points on each build. Newly authored wikilinks fail the build instead of being silently omitted from the graph. Roll back the scoped expansion commit to return to the initial source edition; do not reset unrelated repository history or clear stored bookmarks.


## Reader-depth edition (September 4, 2026)

The third source edition deepens the First Beast, Teacher of Righteousness and Apocalyptic Repair articles without adding unapproved first-person statements. The First Beast article works through dated primary records, the TOR article distinguishes the proposed sequence from textual constraints and elapsed-time calculations, and repair is examined through personal, community and institutional examples. These are attributed editorial syntheses, not independent certification of the hypotheses.

The homepage begins with three flagship questions and links to the remaining eleven developed articles. A new introductory guide and a ten-entry glossary connect the subjects. There are still 14 developed articles and 354 original conversations; the two additional routes are navigators, not newly recovered chats. Source history is available in an expandable section, and exact author-turn links retain source hashes and speaker labels. Print preparation expands source notes before printing.

Global search and the conversation catalogue can filter Micah's top-level turns separately from AI replies. In a speaker-filtered search, every keyword must occur in the same turn. A user turn may contain pasted AI output or quotations; the filter identifies the export speaker, not the authorship of every embedded sentence. Search snippets fetch and hash-check the matching public source bytes, with bounded caching and cancellation of stale requests. The expanded full-text/turn index is fetched on demand, not as a homepage dependency.

The connection explorer now defaults to explained relationships. Editorial relationships and article-to-source relationships have labels and reasons; the complete navigation graph remains available as a separate mode. `data/relationships.json` records 49 explained relationships and five exact author-turn anchors. `data/depth.json` contains homepage choices, glossary definitions and the proposed chronology. Chronology filters separate proposed dates, textual/manuscript constraints and arithmetic; they do not collapse those layers into a single settled history.

The six-record forecast register is unchanged. No prediction outcomes were newly assessed and no missing transcript has been silently reconstructed. Original conversations, legacy source-note Markdown, shared SAN shell, other projects and the private repository are outside the change surface.

Source changes live in `editorial/depth.cjs`, `assets/js/depth-tools.js`, `assets/css/depth.css`, the existing research adapters and `tools/build.cjs`. The expanded tests exercise source integrity, same-turn search semantics, explained graph edges, exact source anchors, responsive layout, source notes, glossary, chronology, and existing browser functions. A passing UI test is not scholarly validation of the article's conclusions. Generated files are reproducible from the source and public archive; never manually edit generated Markdown without updating its editorial source.

## Foundation edition: argument scope and book navigation

The foundation edition adds five developed investigations and extends four constructive articles without replacing their earlier arguments. The canon for this pass is `editorial/foundations.cjs`, `editorial/foundations.json`, and the full Markdown bodies in `editorial/foundation-articles/`. `tools/build.cjs` integrates these with the prior editions and generates all public Markdown and data. Do not edit generated article copies as though they were the canonical source.

The book-contents view contains a proposed seventeen-chapter route across five parts. These are organizational routes, not completed or author-approved manuscript chapters. The parallel-timeline view preserves four separate layers: the June comparison, the stronger October earlier-founder reconstruction, curatorial dates for material witnesses, and textual constraints without an absolute date. Undated records are not assigned fabricated coordinates. Open-ended proposals are marked as such, and manuscript dates are not silently converted into event dates.

The research board contains sixteen specific tasks. Changes are personal browser-local planning overrides, not shared editorial actions, manuscript approval, or GitHub writes. The native select controls work without drag-and-drop. A dedicated localStorage key (`theology:research-board:v1`) stores only known task IDs, selected stages and personal notes. JSON backup/restore merges recognized tasks; invalid IDs/stages and oversized notes are rejected or ignored. Reset affects only this board, never the reading-list key or other storage. Do not put confidential information into a browser on a shared device. No data is sent to a server by the board.

Nine argument-scope dossiers provide twenty-four selected, manually classified passages. These maps preserve specific assertions, corrections, research requests and intellectual debts; they do not imply that every passage in the 354-chat archive has been classified. Top-level speaker labels remain distinct from proposition authorship, since a user turn may contain pasted statements. The Tabor acknowledgment, translation credit, teachers discussed comparatively and an AI-only research lead appear as different records. Later 2026 Tabor posts are current editorial comparisons, not retroactive sources for 2025 statements.

The will comparison also incorporates the author's explicit direction in the current September 5, 2026 editing conversation. That input is identified as such rather than counted as a newly recovered historical transcript. The August 19, 2025 recorded remarks about heaven are separated from the author's further theological inference about motive. No private mental state is asserted as a documented confession.

`tests/research.test.cjs` includes `tests/foundations.test.cjs`. `tests/browser.py` calls `tests/foundation_checks.py` to exercise contents filtering, chronology layers, source-turn links, attribution, stage changes, real-origin persistence, safe backup/restore, storage denial and mobile layouts. All original file hashes are checked again during every build. Existing forecast entries remain unchanged and unscored. No new historical photograph, archaeological provenance claim or independently verified manuscript chronology is implied by the UI.

## Shared long-term roadmap and research-to-book plan

The current plan is canonical in `editorial/roadmap.json`. It contains 36 bounded deliverables in seven phases, 44 prerequisite edges, explicit acceptance criteria, evidence paths and intentionally unset target dates. `Verified output` means that row's bounded deliverable is evidenced; it is not author approval, truth, or manuscript completeness. A build rejects missing references, unknown/self dependencies, cycles and verified statuses without evidence. Work is performed during active editing sessions, not by a promised autonomous background schedule.

The reader's `research-roadmap` route provides a searchable checklist, phase/status/ready-to-start filters, a focused dependency graph with full text alternatives, JSON export, and the Excel snapshot. The existing personal `research-board` remains distinct. Neither Excel edits nor personal board moves update the shared baseline. To update public statuses, edit the canonical JSON in a reviewed repository change, rerun `tools/build.cjs` and tests, and refresh the workbook and its input-hash manifest. Preserve task IDs and the change log; a newly added phase is not retroactive proof of progress.

`source-coverage` reports all 354 conversations, actual developed-article references, associated chapter routes and selected original-turn anchors. An unlinked conversation is not labeled unread or irrelevant. Coverage is regenerated from actual page references; it is not a human-read percentage. The Excel workbook has nine sheets: overview, checklist, dependencies, chapter map, source coverage, textual evidence, intellectual debts, release gates and instructions. Its formulas recalculate local prerequisite and summary counts. `planning/workbook-manifest.json` pins the workbook and all planning inputs; tests reject a stale snapshot. An optional Python exporter uses `artifact_tool` in an environment where it is available, not a required runtime dependency. Recheck formulas and layout after regeneration.

The current substantive addition is a full first comparison of Melchizedek in Genesis 14, Psalm 110, 11Q13, Hebrews 7 and the Nag Hammadi tractate. Passage cards distinguish the consulted translation, a scholar's disputed reading, and unperformed manuscript collation. The article links to the author's original questions at turns 63, 82 and 84 of source 2656 and to the larger Teacher/Thomas/Gnostic reconstruction. Current editorial scholarship and translation credits are not laundered into earlier author influences. The old Melchizedek task advances to citation verification, not completion of the full historical transmission hypothesis. Its next critical-edition and printed-edition work remains explicit.

New source lives in `editorial/roadmap.cjs`, `editorial/roadmap-articles/`, `tools/roadmap.cjs` and the local roadmap reader assets. No source chats or existing individual source notes are changed. The prior six forecast records remain unchanged and unscored. Run `node --test theology-wiki/tests/research.test.cjs` for all checks, including `roadmap.test.cjs`; the native browser suite includes `roadmap_checks.py`. After publication, validate actual hosted files and run the same browser suite against the public origin. Do not publish a claim of hosted success until that run succeeds.

## Roadmap continuation: actual deployment gates

The continued roadmap pass corrects the workbook transfer digest to the preserved workbook manifest and replaces stale test selectors with assertions against the actual reader. Homepage coverage now checks that all developed article destinations occur exactly once across featured and remaining articles. Missing-page tests await the real error state rather than a fixed delay. Workbook download checks require nine sheets and the published SHA-256 digest. A release-tree test rejects leftover transfer placeholders and temporary patch scripts.

The Melchizedek article now separately treats the number of messianic agents and the qualification of their authority. It identifies the compression in the archived AI reply to the author's turn-84 question and credits Dong-Hyuk Kim's 2018 study at the actual abstract-level access scope. The workbook remains the same verified planning snapshot; no task is marked author-approved by this textual clarification.

## Source-atlas edition: shared book and museum foundations

`editorial/atlas.json` and `editorial/atlas-articles/` provide 29 typed source records, six explicitly scoped chronology intervals, four bounded challenge records and three content-only museum trails with eleven stops. The catalogue is a selected starting set, not an exhaustive bibliography or 29 independent ancient works. `tools/atlas.cjs` validates records, article destinations, chapter IDs, original speakers, matching excerpt text and archive hashes. Its generated `data/museum-manifest.json` pins `data/source-atlas.json` by SHA-256.

The two substantial new investigations, Exodus-to-Temple chronologies and manuscript/movement survival, develop existing author arguments. They attach to the existing chapter routes without declaring manuscript approval. A stale canonical paragraph now links the written Melchizedek comparison. The selected firstborn passage restores an explicit distinction the author had already made; the institutional challenge distinguishes the author's proposal from the AI's narrower restatement. Favorable AI chronology is audited too, not treated as archaeological corroboration.

`source-atlas`, `argument-challenges` and `museum-trails` are ordinary reader routes with generated Markdown alternatives. The source view filters record kinds, compares records, opens explained relationships and exports JSON. The chronology experiment keeps foundation and destruction anchors separate, validates input, and exposes ordinal versus elapsed counting. Its example dates are assumptions, not adopted historical dates. All new code and styles are local and dependency-free.

No XR runtime is modified or claimed delivered. The handoff in `planning/BOOK-MUSEUM-ATLAS-HANDOFF.md` describes stable identities, source fidelity and the work remaining for the separate XR consumer. All exhibit asset lists remain empty until reuse permission and correct object identity are checked. Existing museum pictures are preserved unchanged.

The 36-task shared plan retains its IDs and dependency graph. Two source-development tasks advance to Draft, not completed research. The nine-sheet Excel snapshot is refreshed to the current plan, chapter routes and source anchors with all 942 formulas preserved and no formula errors in the inspected export. Original chats, individual source-note pages, existing forecast records and all files outside theology-wiki remain untouched by this edition. `tests/atlas.test.cjs` and `tests/atlas_checks.py` extend the existing checks; passing them establishes bounded data and interface properties, not truth of a reconstruction.

## Listening and multi-format products (September 6, 2026)

The `listening-room`, `production-studio` and `product-pathways` routes add a public listening application and source-linked editorial production series. The entire body and headings of all 22 developed articles are normalized into bounded narration segments by `tools/products.cjs`; source/visual notes have separate flags. Each narration record pins the original Markdown and the library pins each narration record. Nothing is copied from the private SAN manuscript. The private reader was supplied as a reference; deployed audio parity is not claimed.

The listening app provides article and proposed-chapter filters, section and paragraph navigation, speed, sleep timer, optional source notes, and browser-local source-versioned positions. Device voices vary by browser and OS. Only local voices are listed until the reader explicitly includes remote device voices; a remote provider may receive spoken text. No paid speech API, microphone, account, telemetry, or voice clone is used. Browser speech is not an MP3 export and the UI says so. A lack of voices or a text-hash mismatch produces a visible fallback. Navigating away stops owned audio and voice playback.

`editorial/products.json` is the canonical six-format product register, ten delivery tasks and three episode scripts. These supplement the unchanged 36-task research roadmap and unchanged nine-sheet workbook; no research gate is approved by shipping software. `editorial/product-guide.md` develops the shared editorial purpose. Episode text is a third-person editorial adaptation, not an author quotation or an approved first-person manuscript. Stronger succession, displaced-biography and institutional claims remain in the Teacher episode. Full sources and hashes accompany script exports.

`tools/render_product_media.py` produces a synthetic draft full-study recording and a narrated typographic video pilot, using the pinned CPU Kokoro model/voice and no external imagery or music. It emits real MP3/MP4 files, segment-time WebVTT captions, spoken transcripts and hash manifests. Its model and speech dependencies are producer-only, not site dependencies. No model weights or font files are redistributed. The full-study recording includes all main-body paragraphs and headings, with separately labeled source/visual notes excluded; the complete text download retains those notes. Caption timings measure generated segments, not words. Final voice, names and abbreviations need listening and author review.

Normal builds validate installed recordings against their script/source hashes and file hashes but do not regenerate speech. An article change invalidates its old recording rather than presenting it as current. `tests/products.test.cjs` has isolated speech-controller fixtures; `tests/products_checks.py` uses native HTTP and actual recording playback. A browser without device voices is tested as unavailable, not falsely passed as successful TTS. Physical-device screen lock, Bluetooth and pronunciation testing remain separate tasks.

Technical references: MDN SpeechSynthesis getVoices, SpeechSynthesisVoice localService and SpeechSynthesis cancel, consulted September 6, 2026; https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesis/getVoices ; https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesisVoice/localService ; https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesis/cancel . Production model and API: https://github.com/hexgrad/kokoro and https://huggingface.co/hexgrad/Kokoro-82M/tree/30618d04b530efb8e3ac3bace784f9d4a8dcaa01 . The source atlas and museum content manifest retain their own unchanged research version.

Recorded media progress is saved separately from device-reading paragraphs, pinned to the same source hash. Native audio and video stop competing players when started; seek/pause/end update the local recording position. Reload does not autoplay. Both audio recording and device reading use the selected reading speed. Backups restore only known, current-source positions; denied browser storage keeps the controls usable with an explicit session-only notice. This is not a guarantee of physical-device background or lock-screen playback.

For a host whose media reports no usable seekable range, a saved recording can load a full same-origin copy after a media request. That fallback checks the actual byte count and SHA-256, is bounded to 24 MiB, and keeps the saved time until seeking succeeds. It may download the file again. Object URLs and pending fetches are released on navigation. It does not bypass authentication, create synthetic test audio, or promise streaming on every host.

## Authorial formation edition

The working name Christic Self-Formation Theory keeps the stable christ-as-an-inner-model route. All 23 developed articles now use canonical first-person/theory-centered bodies in editorial/authorial-articles, with metadata and 14 newly consulted references in editorial/authorial.json. Earlier article bodies remain legacy editorial records, not current build inputs. See editorial/AUTHORIAL-VOICE.md before editing. Archived conversations, quotes, speaker labels and source bytes remain unchanged.

The expanded argument distinguishes an embodied bearer, a learned self-model and a momentary state; inward presence, two-level correction and bounded choice; and a comparative not-self reading without asserting doctrinal identity or historical transmission. Three original neuroscience studies delimit relevant comparisons rather than certify the theological mechanism. Private SAN chapters informed this synthesis but no private manuscript is redistributed.

A new historical investigation and five-stop content trail connect inward covenant, Qumran discipline and Sabbath songs, Pauline formation language, P46 and the Dura-Europos ritual setting. Twelve new typed atlas records and twelve edges distinguish works, copies, modern studies and hypotheses. Four added timeline records retain broad or unknown dates where the consulted sources do not warrant numerical bounds. Both 2024 Dura interpretations remain visible; the Pettegrew access record is abstract-only. The new trail is a source-linked content handoff, not a newly implemented XR runtime.

New article wording and first-person episode scripts require regenerated media, not reassigned hashes on earlier third-person audio. Source-pinned listening derivatives and affected stock-voice recordings are rebuilt together. This editorial point of view is requested; final voice and pronunciation approval remain open. The portable planning snapshot is refreshed to the current chapter and source links without marking additional research tasks complete.

## Connected computational and historical foundations

The first-person edition is recovered and tested before publication, and the two research-intake drafts become full canonical articles in editorial/authorial-articles/. Computational Divine Immanence remains a working theory label. The computational-argument-map route and data/computational-foundations.json expose ten distinct argument connections, not numerical truth scores. The 17-source register retains original access limits. Personal journal and relationship material are not reproduced.

The source atlas gains thirteen records and twelve typed connections; two content-only museum trails add eight stops. The book still has seventeen proposed chapter routes, now linking both investigations. Device narration is generated for all twenty-five articles. The two existing recordings remain source-pinned to the recovered first-person edition; the new articles are not advertised as prerecorded. Existing archives, forecast observations, private SAN files, XR runtime and unrelated applications are unchanged.

The research plan advances only the bounded coherence and formal-comparison tasks to Draft and records remaining empirical and textual work. A revised nine-sheet workbook must match the regenerated inputs. This is not author approval of the whole manuscript or confirmation of a reconstructed event.
