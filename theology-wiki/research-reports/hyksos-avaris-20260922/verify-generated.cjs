'use strict';
const fs=require('node:fs'),path=require('node:path'),crypto=require('node:crypto'),cp=require('node:child_process'),assert=require('node:assert/strict');const root=path.resolve(__dirname,'../..'),repo=path.dirname(root),base='c31dd6c56a101aec7c8a890768ae1f845494b294',load=p=>JSON.parse(fs.readFileSync(path.join(root,p))),hash=b=>crypto.createHash('sha256').update(b).digest('hex'),sha=p=>hash(fs.readFileSync(path.join(root,p))),old=p=>cp.execFileSync('git',['show',base+':theology-wiki/'+p],{cwd:repo,maxBuffer:32*1024*1024});
const slugs=['moses-volcano-and-exodus-chronology','exodus-to-temple-competing-chronologies','kenite-hypothesis-and-yahweh-origins'],articlePaths=slugs.flatMap(s=>['content/developed/'+s+'.md','data/listening/'+s+'.json','products/transcripts/'+s+'.txt']);
function authorized(){const baseline=load('tests/fixtures/restoration-baseline-hashes.json'),episodePaths=[];for(const e of load('editorial/products.json').episodes.filter(e=>e.pages.some(s=>slugs.includes(s))))for(const ext of ['json','txt']){const p='products/episodes/'+e.id+'.'+ext;if(Object.hasOwn(baseline.files,p))episodePaths.push(p);}return [...articlePaths,...episodePaths].sort();}
function verify(){const baseline=load('tests/fixtures/restoration-baseline-hashes.json'),priorFiles=['scripture-concordance/revision-receipt.json','research-expansion-20260918/revision-receipt.json','comparative-religion/revision-receipt.json'],combined={};let count=0;
 for(const p of priorFiles){assert.equal(sha(p),hash(old(p)),p);const r=load(p);for(const[f,v]of Object.entries(r.files)){assert.ok(!combined[f]);assert.equal(v.before,baseline.files[f]);combined[f]=v.after;count++;}}
 assert.equal(count,18);const current=load('research-reports/hyksos-avaris-20260922/revision-receipt.json');assert.equal(current.base_commit,base);assert.deepEqual(Object.keys(current.files).sort(),authorized());
 for(const[f,v]of Object.entries(current.files)){assert.ok(!combined[f]);assert.equal(v.before,baseline.files[f]);combined[f]=v.after;}
 for(const[f,v]of Object.entries(baseline.files))assert.equal(sha(f),combined[f]||v,f);
 for(const e of load('editorial/products.json').episodes){const p='products/episodes/'+e.id+'.json',before=JSON.parse(old(p)),after=load(p);delete before.sources;delete after.sources;assert.deepEqual(after,before,'Episode speech unchanged '+e.id);const t='products/episodes/'+e.id+'.txt',strip=s=>s.replace(/SHA-256: [a-f0-9]{64}/g,'SHA-256: [version]');assert.equal(strip(fs.readFileSync(path.join(root,t),'utf8')),strip(old(t).toString()));}
 for(const a of JSON.parse(old('products/media/manifest.json')).assets)for(const f of a.files)assert.equal(sha(f.path),f.sha256,'Recording unchanged '+f.path);
 return {prior:{legacy:{unchangedBaseline:baseline.baseline,priorDeclared:6,newDeclared:9},comparativeDeclared:3,totalDeclared:18},hyksos_article_derivatives:9,hyksos_authorized_derivatives:authorized().length,total_authorized_derivatives:18+authorized().length};}
if(require.main===module){if(process.argv.includes('--write')){const baseline=load('tests/fixtures/restoration-baseline-hashes.json'),files={};for(const p of authorized()){assert.equal(hash(old(p)),baseline.files[p],'New derivative must not replace an earlier revision '+p);files[p]={before:hash(old(p)),after:sha(p)};}fs.writeFileSync(path.join(__dirname,'revision-receipt.json'),JSON.stringify({schema_version:1,archived_on:'2026-09-22',base_commit:base,scope:'Three linked articles and their narration/transcript derivatives, plus any pinned episode citation-hash derivatives. Episode spoken content, recording bytes and all previous revision receipts remain unchanged.',files},null,2)+'\n');}console.log(JSON.stringify(verify()));}
module.exports={verify};
