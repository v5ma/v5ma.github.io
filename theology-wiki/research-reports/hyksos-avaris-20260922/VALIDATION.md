The original report and all citation metadata were recovered from the completed supplied research export. Only final report text and source metadata were selected; private activity and other conversation material are excluded. Four source checkpoints were committed before build work.

Initial runner 35803013678 verified the exact exported JSON and original body SHA-256, restored 68 citation placements and 26 source records, and rendered all nine report tables. It then stopped because the existing main Wiki builder could not load C.safeSourceFile. No complete success is claimed for that run.

The inspected repository root package.json sets type=module for Cloudflare, while the existing Theology research-core.js is a CommonJS/UMD helper without ES-module exports. A new theology-wiki/package.json scopes CommonJS to the Wiki's existing .js helpers. It leaves the root package, Cloudflare configuration and browser code unchanged.

The next run, 35803099486, reached the recording-source check and correctly refused to relabel older recordings as recordings of updated articles. The existing media files and original manifest were preserved. Two exact earlier source versions are saved in recorded-source-snapshots/. The builder verifies their original hashes and labels the affected assets as archived recordings of those versions. New report links are not claimed to have been recorded. The playback test now compares recorded cues with the correct verified source version rather than dropping the correspondence check.

Run 35803399062 passed all nine report-specific tests and all 159 principal Wiki tests with zero failures and zero skips, then committed generated output as 657fbee863a2bb4ca05a99de6b906b77c9fd28a3. The actual artifact 10726004708 was downloaded and inspected; SHA-256 4812fc0293a3e749ab41ba1d6396dee126675c256ab3c61a5864a79bba9acedb matched the provider digest. The report source text and all citation placements were verified, not inferred from a successful workflow label.

The preservation checker was then made usable in shallow checkouts by retaining explicit base-version pins rather than requiring remote history during every legacy check. Run 35803538722 again passed the report and principal tests and committed 12eea544f4736f666b6d4fcdaed513225909b451. Prior revision receipts, original source conversations, forecasts, all unrelated authored articles, recording bytes and episode spoken content remain unchanged. Three canonical articles have append-only report links and their full reader, search and narration-text derivatives are regenerated. Episode references receive updated article hashes, not rewritten narration.

The one-shot branch writer is disabled. Persistent validation is read-only and unscheduled. Native browser completion, merge and actual public-byte and reciprocal-link verification are recorded in the pull request after those steps run. Rendering and UI checks establish source fidelity and delivery, not historical proof of the report's interpretations.
