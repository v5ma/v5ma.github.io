# First screen inspection: one architecture refinement required

Both original PNGs were opened at full resolution. The results figure is legible and its counts match the measured metrics. The architecture diagram's prefill arrow pointed only at the central recurrent-memory box; the convolution and attention boxes should also receive an explicit branch from the native model. This is a diagram-routing defect, not an experiment defect.

Preserve this first figure generation and its source/receipt. A separately named v2 figure directory will show all three state outputs. Do not use this first architecture PNG in the current manuscripts. No PDF layout acceptance is asserted.
