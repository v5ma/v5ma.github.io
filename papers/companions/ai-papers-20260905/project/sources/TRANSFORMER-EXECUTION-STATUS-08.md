# Transformer execution status — Draft 8

September 5, 2026. The user's GPT-2/modern open-model requirement remains an acceptance condition, not a title change alone. The [architecture contract](TRANSFORMER-BRIDGE-AND-ACCEPTANCE-04.md) remains authoritative at its stated source depth.

## Executed

The exact Xenova GPT-2 ONNX revision and quantization from the [intake receipt](../model-dependencies/gpt2-xenova-bf2c7f02/LOCAL-INTAKE-RECEIPT.json) now supports static residual intervention, an actively learned twenty-coefficient correction layer, a varied-intent/attribute assay, and actual cache-carried continuations. No base GPT-2 weight update is claimed. The correction layer's known representation-editing algebra and stronger ReFT/RAVEL comparisons remain explicitly credited.

The [Draft 8 review](../reviews/gpt2-continuation-08/REVIEW.md) records eight serial one-thread runs: 228 actual root outputs plus 656 continuations, including sixteen fresh post-request continuations. The 884 actual outputs are distinct from root-table reuse. Sixty-one cumulative tests and all eight separate-code native checks pass. Twenty signed-request store events distinguish future-edit stopping from restoration of already changed context.

The [cache methods check](CACHE-CONTINUATION-METHODS-08.md) connects exact GPT-2 implementation ordering to the measured layer 9–11 versus layer 0–8 restoration controls. This is a verified known computational route, not a new transformer architecture or mechanistic-interpretability algorithm. Restoring a known cache is not learned reconstruction of unknown history.

## What did not become a successful semantic demonstration

The unedited model answers the original-giver follow-up correctly in 0/32 cases; the reversed-prefix donor also fails the intended later-giver test. Joint-score zeros therefore cannot discriminate semantic-intervention failure from baseline incompetence. All predictions are retained, and [baseline adequacy](../reviews/gpt2-continuation-08/BASELINE-ADEQUACY.json) is explicitly post-hoc. The experiment was not reworded or filtered after seeing results.

The earlier stronger positive findings are not discarded: the original recipient baseline gets 24/32; the separate Draft 7 task-edit color readout gets 32/32. These are different question/state conditions, not a cross-draft learning curve.

## Still mapped or proposed, not executed

- No DeepSeek original MoE, DeepSeek V4 or instruction-tuned contemporary-model experiment has run here. Architecture mapping is not empirical transfer.
- A dense distilled model must retain its actual base architecture and provenance label; it cannot stand in for original DeepSeek expert routing.
- No measured intelligence-gap scaling, reduced human oversight, learned natural-language scope semantics, endogenous access or robust relational self-regulation is established.
- No independent review, complete new pipeline exact replay, final manuscript PDF or release is certified.

## Next discriminating gate

Use development-only material to verify basic relation/attribute competence before freezing untouched evaluation cases. Select a small contemporary instruction-capable open-weight target only after checking the exact model, license, implementation access, dependency size and serial resource ceiling. Preserve the existing GPT-2 adapter for reproducible controls; do not multiply exposed diagnostic variants just to increase counts. Original-model hooks, matched observation/query budgets, baseline-preserving interventions and clear cache/parameter/authority boundaries remain required.

No bulk download or high-resource run is implied. All new code, data and prose stay in this D: paper root; no Book 2, source-original, other-task or provider mutation is needed.

