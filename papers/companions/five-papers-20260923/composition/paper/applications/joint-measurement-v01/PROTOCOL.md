# Joint configuration measurement and the readout-only alternative

23 September 2026. A new exhaustive **measurement diagnostic**, not execution of the learned experiment in manuscript Section 6.8. No trained parameters, development/test split, learned controller or biological result will be claimed. Original programs and results remain unchanged.

Use the proposed arena task's typed records: two actors, two voice identities, two earlier four-valued commitments, two current entrance states and self-location. Enumerate high token bin 0/1, opposite within-bin low bits for the two commitments, both speaker assignments, both queried actors, four entrance-state pairs and both self-locations: 128 base episodes. Four event times label the old two commitments and current vision/proprioception. This is a relational-memory surrogate, not raw speech or imagery. The route action is the queried actor's token low bit XOR its current entrance state XOR self-location. The actor/voice binding, not token frequency alone, determines which token is used. A separate management endpoint returns the high token bin and current entrance relation.

The assembly records C (present payload identities), B (actor-to-voice edges), Pi (exact tokens, two-bin tokens or absence), and H (represented earlier/current event lags). The archive is reset each episode. A hard-coded constructor, not an answer-trained policy, makes these configurations. The query interface returns only the constructed assembly to the action rule.

Cross fine/coarse and earlier/current-only availability. Swap only B when its old payloads are available; verify the other fields remain byte-identical. Coarsening changes Pi and token values but preserves their ownership. Removing old content also removes old B and H; count it as a joint change, not an isolated temporal intervention. Restore the saved state and check the exact original probability. For unavailable low bits output probability 1/2, with no archive reopening.

Compute all four E_B terms and J as defined in Section 6.8. Verify fine/earlier E_B=1 and the other terms zero, intact action accuracy 1, and exact rescue. Conditional label balance in each coarse/current observable must be exact. These are algebraic predictions of the constructed task, not held-out learning targets met by training.

The stronger countermodel stores an unchanged complete episode and applies the same access, coarsening and binding operation only at readout. Its full encoded state stays unchanged; its observable probabilities should equal the assembly-changing implementation. Charge this full record explicitly as a storage-rich countermodel, not a payload-matched competitor. Direct state hashes distinguish the constructions, but action/J alone must not. This sharpens the necessity of independently measuring assembly instead of inferring it from behavior. A secondary counterexample is not a refutation of the physical hypothesis.

Run one thread, no network, explicit directories, at most 2 MiB output. Preserve code/protocol hashes and all finite cases, run twice, compare bytes. No learned Section 6.8 threshold, novel biological control law, or neural experience conclusion follows from this diagnostic.
