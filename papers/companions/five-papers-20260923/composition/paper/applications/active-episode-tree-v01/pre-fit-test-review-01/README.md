# Pre-fit test correction

The initial 40-check receipt is preserved as `../TESTS-01.json`. One role-independence assertion merely compared a function call with itself and therefore did not test its stated property. Before any declared model fitting or evaluation, it was replaced by two checks: role triples are constant within each eight-case background block, and the selected first role leaves the balanced temporal target undisclosed. The new acceptance receipt is `../TESTS-02.json`. The prior test, training entry point and protocol are preserved here byte for byte; they are historical evidence, not runnable current inputs. The protocol also replaces the imprecise phrase "requested precision" with "requested granularity". No model or outcome was selected using this correction.
