# Accepted visual readback

19 September 2026. Same authoring agent, not independent review.

The result chart was opened in its first generation; the second-generation copy is byte-identical at SHA-256 `9a5b02c3c9137cfa59ebd0ad1bb0121237c57bfb870e821e9eef1e1a38f259d5`. All eleven row labels, support categories, query counts and mean position errors are readable. The footnote distinguishes retained knowledge from fresh evidence, reports unseen-swap errors, and preserves the direct comparator's equality and the fixed survey's distance advantage. No biological error bars or inferential animal statistics are implied.

The revised system schematic was separately opened and inspected at its delivered resolution. Its subtitle no longer overlaps the teaching box. Actual returned evidence has its own route into the relation, rather than sharing the outgoing action arrow. All box text and arrowheads are readable; the evaluator-only route has no return into the controller. The figure explicitly identifies its status as a schematic and keeps the uncompleted native/full-SAN claims out of the caption. SHA-256 `cfb59f24c4a9018cde2ad488c33db78530163f6330a1d6399aae9fcbf966b3b2`.

The rejected first schematic and its readback remain in `figures-01`; the source data and model did not change. These PNG checks are not approval of a paginated publication PDF.
