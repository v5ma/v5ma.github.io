# Exact receiving witnesses to action and returned gain

23 September 2026. Frozen before execution. This is a new synthetic intervention diagnostic on the already pinned source matrix, not raw fly physiology, a trained SAN brain, or a published-method benchmark. No new source download or SVD choice is used.

Read the exact 79-by-147 NPY export at the existing source intake, check its recorded hash, and use these fixed contrasts: e22-e23 in the count export; e28-e32 and e22 in the notebook's first-target-removed, strictly-greater-than-five binary representation. Receiving rows are normalized by their sum. Patterns are 1 +/- 0.2 v. The selected pre-mixing intervention doubles the first nonzero contrast coordinate, followed by global Frobenius-norm matching. The diagonal intervention is an assumed controllable port, not established biological APL action. Cell-wide post-mixing doubling and an honestly acknowledged undelivered request are controls.

Compare genuinely different observers at the same two observations and installed calibration pairs. A conservative set observer intersects labels compatible with the finite noise support. A Bayesian observer multiplies exact likelihood masses and chooses the minimum expected loss action: wrong sign costs 1, abstention costs 1/4, correct sign costs 0. Neither controller receives the hidden sign. The Bayesian observer additionally exploits declared likelihood weights rather than just their support; these weights are supplied equally to both implementations but the set rule deliberately does not use them. This is a difference in decision objectives/statistical use, not evidence of architectural novelty or superior biological knowledge.

Noise lies on the declared receiving contrast direction, with integer offsets {-2,-1,0,1,2} and masses {1,2,4,2,1}/10. Its scale is half the calibrated selective separation, fixed across all conditions of that witness. For the permanently invisible witness use scale 0.001 and the first receiving coordinate. Enumerate both hidden signs and all 25 noise pairs, with exact probability weights, in selective-pre, common-post, fixed and request-denied conditions. The requested and actual operator are distinguished. Calibrated null differences are treated as zero at 1e-10; this tolerance is not a physiological threshold. Conditions are artificial and noiseless calibration is generous.

After the two received observations, the selected sign drives a simple actuator. Its actual gain is alternately 0.8 or 1.2. Starting estimated gain is 1; returned movement divided by nonzero command updates that estimate halfway toward the observed gain. A second command uses the new estimate. Report classification, abstention, actual first/second movement and gain estimates. This joins an information-changing receiving intervention to action and proprioceptive-style feedback in software; it is not a whole fly body or multimodal rendering. Reset between cases. No plasticity or sample-trained query policy is claimed.

Prediction: selective pre-mixing can expose the two recoverable contrasts; cell-wide post-mixing cannot restore either initial null contrast, and the zero-route witness remains invisible. Bayes and set rules can differ on ambiguous but unequal-likelihood observations, so the comparator is not the historical norm-versus-squared-norm equivalence. Wrong actions under the Bayesian loss rule must remain reported alongside abstention and expected cost. For nonzero decisions, returned gain reduces the second command's error relative to its chosen target; that cannot turn an incorrectly inferred target into the right target.

The finite noise-law assumptions, installed calibration and arbitrary port control bound every conclusion. No independent empirical replication, confidence interval, novel general theorem or biological confirmation follows. One CPU thread, under 120 seconds, at most 5 MiB output, fixed paths, no overwrite. Replay byte-for-byte and independently audit the probability arithmetic.
