# Receiving intervention, distinct observers and returned action

[Protocol](PROTOCOL.md) · [source](run.py) · [all results](run/RESULT.json) · [byte-identical replay](replay/RESULT.json).

This added construction uses the pinned projection export, two exact recoverable equal-route contrasts and a permanently invisible zero-route contrast. It does not select an arbitrary singular vector. All historical source files and experiments remain unchanged.

For either recoverable contrast under selective pre-mixing receiving, the conservative two-observation set rule has probability 0.51 correct, 0 wrong and 0.49 abstaining. The distinct likelihood-weighted Bayesian rule has 0.83 correct, **0.05 wrong** and 0.12 abstaining. With declared wrong/abstention costs 1 and 0.25, expected losses are 0.1225 and 0.08. These are exact expectations under the supplied discrete noise law, not sample frequencies, animal error rates, confidence intervals or a generic SAN advantage. Both observers receive the same observations, installed prototype calibration and available probability law; the conservative rule uses only the support. The objectives differ deliberately.

Common post-mixing gain, fixed receiving and honestly denied queries leave both original null contrasts unresolved. The zero-route witness is unresolved even under selective receiving. All these controls abstain with probability one. The two recoverable witnesses have different physical matrix separations, but noise is scaled to each witness's calibrated selective separation; their matching performance is therefore not an anatomical robustness comparison.

The selected action drives a simple actuator with gain 0.8 or 1.2. Its measured movement returns to update the gain estimate before a second command. First movement magnitudes 0.8/1.2 become 0.888889/1.090909, closer to the **chosen** unit target. This motor calibration cannot repair a wrongly selected sign. No request alone changes the receiving operator, and no hidden sign enters either observer. Reset occurs between cases; this is temporary actuator adaptation, not retained synaptic plasticity or a trained query controller.

The producer verifies 3,639 finite arithmetic/contract checks and preserves all 1,200 observer-case records. Replay is byte-identical. The later cross-paper audit recomputes probability and motor-return arithmetic without importing this producer. All validations are automated work in this task, not external scientific review.

F04 is advanced by joining a declared receiving change, actual acknowledged observations, differing decisions, action and return. Physiological input-port controllability, signs/weights, biological noise, native timing, learned regulation and the whole PWD/NAPOT/multimodal loop remain unestablished. The diagonal intervention and calibrated prototype repertoire are explicitly installed assumptions. No new data download, source edit or publication occurred.
