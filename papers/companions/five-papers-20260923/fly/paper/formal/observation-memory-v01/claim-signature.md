# Human-readable claim signature

Claim: **SAN-FLY-M7A-M8-OBSERVATION-MEMORY-20260919**. Status: **yellow**, with exact compiled evidence and review still open.

The seven named lemmas in [the JSON signature](claim-signature.json) establish state-history equivalence and a specified retained/temporary memory reset contract. Equal whole initial state and equal permitted inputs under the same deterministic functions imply equal state/action histories. Clearing only the temporary field preserves retained state and its declared readout. Commutation applies only when the learning update does not read temporary state and receives the same body return.

The complete controller object—not `state()`'s five-field summary—is the candidate mathematical state. The [interface audit](APPLICATION-INTERFACE-MAP.md) documents that distinction and tests the existing code. It does not certify a formal Python refinement.

M7b, the metric half-separation result, is **not** in the compiled inventory. Its analytic proof and finite witnesses are retained, along with the unsuccessful time-bounded compilation attempts. M1–M6 also remain analytic rather than being silently included in the seven compiled-lemma count.

No theorem here establishes biological fidelity, a SAN-specific advantage, experience, immunity to arbitrary software side channels, or independent expert review. The next consumer is the source-registered SAN receiving/circuit prototype, where state ownership and permitted evidence must be made explicit before comparison.
