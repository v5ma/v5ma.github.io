# Prism Coherence Pass4 / host0.11.3 verification

The accepted runtime is52b054d1a29411d5f1d302cbefb791aacb9eb89f. Workflow35675405642 passed its independent source and public jobs. All117 public file hashes matched. Each job passed renderer-policy11, trees22, fire24, water13, interruption19 andRotunda73, totaling162 native checks, with no captured JavaScript/shader errors. Both normal Arcade battles completed in both broader journeys. Exact receipts, results and hashes are in modules/environment/PUBLIC-PASS4.json.

The downloaded source archive matches eleven selected local runtime hashes. Its Node suite passed322 tests with no skips or failures. The actual bundled-Three object checks passed coherence19, trees83, fire64 and water37. Controlled renderer collaborators exercise ownership and restoration but do not execute a GPU draw. Local Chromium WebGL2 remained unavailable; native browser rendering occurred in GitHub Actions.

Renderer policy has an explicit negative case. Only the former sync implementation is substituted in an isolated page, producing four redundant same-size setPixelRatio calls from four actual preference changes. The repaired page produces zero at the identical320x250 drawing resolution. Start, six blade swaps, actual cutting/destruction and pause preserve that buffer. Genuine Light/Balanced changes still apply.7 and1. No game state, score, actor or clock is assigned to produce the repaired result.

This confirms a specific unnecessary reset was removed. It does not prove that all earlier frame stalls had one cause. The first repair-only source checkpoint still paused at21.0897s after a438.5ms gameplay renderer call, although its public suites passed. The final coherent source/public runs passed without that failure. Earlier source/public artifacts remain in the receipt rather than being silently discarded or repeatedly rerun into a green history.

The test-only frame tracer keeps bounded records of CPU tick, art/menu update, rendering submission, input, pause, resizing and long tasks. It does not run in the normal game entry, own input or advance time. Submission duration is not GPU completion, and an unattributed inter-frame gap is not a proven diagnosis.

The new bank meshes share their authored height profile with water bed samples. Object tests inspect every ground/water vertex, upward normals, actual tide uniforms, tree grounding, borrowed texture ownership, AR/Mothership hiding and disposal. The geometry has7536 triangles versus10944 previously, but three bank draws instead of one; no hardware speedup is inferred from those budgets. Full1440x1000 source/public entry images were inspected separately from gameplay correctness.

The original0.35-second safeguard, successful-action requirements, input mappings, audio, core rules and records are intact. All48 baseline browser/runtime entries were compared; only the entry, app synchronization, art and bank-tree placement changed. Other games and concurrent repository work were preserved.

Physical Quest/Xbox/touch, ordinary-resolution frame-time percentiles, thermals, stereo comfort, unfamiliar-player readability and owner approval remain open. AR and hand-menu emulation do not establish hand-only combat. The private hub and combined brief remain outside public source. The final closeout modifies evidence/docs only, not the runtime tested above.
