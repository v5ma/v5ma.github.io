# Prism Rotunda Recovery 0.11.1 verification

INTEGRATION-AUDIT.md records the current branch/source reconciliation. INTERRUPTION-RECOVERY.md describes the 0.11.1 asynchronous playback patch. ROTUNDA-QA.md and ROTUNDA-RECOVERY.md retain earlier interface evidence and revealing failures. Historical results are not the public acceptance record for a different source commit.

The owner requires direct-master writes, not a PR-based release process. Reconcile the latest head, write only the intended Prism paths without force, and inspect the exact resulting commit. Existing historical branches do not need to be merged again: the audit found all 14 Prism tips already included in master.

The existing read-only Rotunda workflow has independent source and public jobs. The source job runs against local HTTP on the checked-out commit even when Pages is behind. The public job separately compares every manifest-listed Prism file with served SHA-256 hashes before running the native tests against the live site. Both must pass for their respective claims; a source pass is not a live pass, and a public hash failure must not suppress the local-source evidence.

Within either job the delayed-audio interruption suite and the complete Rotunda journey have independent steps and reports. A failure in one remains a failed job and cannot be hidden by the other passing. The rendered journey covers actual scene buttons, transformed panel picking, two full battles, sound adjustments, Xbox navigation, strict native-shaped AR/VR input, hand menus, stable placement, room movement, water opacity, wrist/floor status, session exit/re-entry and preferences. No score, health, position, boss state or clock is assigned to manufacture acceptance.

F2 remains the explicit semantic-control alternative. Compatibility suites choosing it do not replace the untouched in-canvas entry test. Classic suites continue at rhythm.html and do not prove new River features.

The audit's exact baseline passed 250 local model/lifecycle tests. The public failure artifact from run 35556669422 showed seven of 83 expected files still reflecting 0.11.0 and skipped its native replay. It must not be counted as a passing 0.11.1 publication. Consult the resulting direct commit's workflow artifacts for later actual outcomes.

Physical Quest/Xbox/touch, long-session GPU/thermal behavior, ordinary-resolution combat performance, unfamiliar-player readability and owner approval remain separate open obligations. Reduced software-rendering buffers provide input/lifecycle evidence; full-resolution captures are not sustained hardware benchmarks. Rollback must revert only the appropriate Prism changes on current master without deleting saved progress or concurrent sibling work.
