# Prism Current / environment Pass 3 verification

The current host is0.11.2 with Water0.1.0,Fire0.1.3 andTrees0.1.3. The exact tree-pass runtime is18d83fa4b5aa2f4115d6c602af2bf9a51c06896a. modules/environment/PUBLIC-PASS3.json records inspected source/public artifacts and the unchanged source retry. The public job matched113 files and passed trees22,fire24,water13,interruption19 andRotunda73, including both complete Arcade chapters. It does not erase source failures or establish physical-device approval.

The recovered runtime independently passed306 local Node tests and bundled-Three object observations: trees83,fire64,water37. Three new module-index consistency tests bring the documentation closeout count to309 without changing browser code. Controlled preparation collaborators do not perform a GPU draw. Local Chromium WebGL2 was unavailable.

The existing read-only workflow reports checked-out source and public gameplay independently. A public hash failure must prevent a public gameplay claim, not suppress source evidence. Every report identifies its exact commit, retry and input scope. Native game scenes and isolated shader/geometry fixtures remain separately labeled.

Tree acceptance tests the ordinary eight-tree entry, actual quiet/quality controls, slicing and destruction with water/fire/trees together, frozen paused wind and encounter state, bounded resources, Mothership hiding, AR transparency, VR detail cap, world anchoring and exit. Separate full-resolution entry and preset images are visual inspections, not sustained performance tests. The full Rotunda journey tests two real-time battles and scene controls without assigning score, health, time or actor state.

The first shared-wind source attempt recorded a470ms renderer call around21.17s, the unchanged rendering-stall pause and a separate initial panel-transform assertion failure. One unchanged retry passed allRotunda73 and both battles, fire24,water13 and models/objects. It still failed the dedicated tree test after a pause at7.6395s, and the interruption suite after nine AR checks when an empty VR battle paused at0.1017s. No shader/script errors were captured. The retry tree log contained no >200ms gameplay renderer call, so the full-frame gap cannot simply be attributed to the same measured draw as the first attempt. See TREES-PASS3-RECOVERY.md for exact artifacts and the next reproduction task.

Preserve all failures. Do not auto-resume, assign gameplay state, lower required successful actions, weaken scoring/damage or remove the timing safeguard to produce a green badge. Source/public differences need bounded full-frame diagnostics and actual input history before a runtime change.

Physical Quest/Xbox/touch, ordinary-resolution frame-time percentiles, thermals, stereo comfort, unfamiliar-player readability and owner approval remain open. The complete user brief and private hub remain outside the public game. Its outstanding color-switch/base-reward/bonus/hazard-radius requests are not implemented by scenic trees.

INTEGRATION-AUDIT.md, INTERRUPTION-RECOVERY.md, ROTUNDA-QA.md and earlier module receipts retain historical evidence, not claims about every later runtime. F2 remains the semantic-control alternative; legacy suites remain at rhythm.html. Direct-master writes must reconcile concurrent work and preserve saves, sibling games and private boundaries.
