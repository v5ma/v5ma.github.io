# Friendly Current 0.12.2 / deliberate Resume must not disappear

The independent AR test in run35747362115 on577f9d60 demonstrated actual block damage, tracked laser healing, tracked purple-block slicing, laser block destruction and immutable paused difficulty before failing to resume. Source artifact10703723771 was downloaded and verified against SHA-256c8d47f6140f670596cde0e9e98eb4e3a4f044c4bd1db574449f7fb151bb75caa. Its 10 completed checks and no-error snapshot are retained. The battle stayed paused at34.5311s with health100, one real block cut, two successful laser hits, three damage and three healed. The menu action counter remained8 after the Difficulty Back action. This was not a failed healing rule or a stall-triggered pause.

The actual XR dispatcher applied its120ms duplicate-pointer debounce to every action, including a new physical B/Y command immediately after Back. The button edge was consumed but its Resume action was ignored; merely holding the button did not create a second edge. The new code distinguishes the direct B/Y edge from pointer selection. Direct start/resume bypasses only that previous-pointer debounce, while session visibility, busy/calibration/controller checks, one-action-per-frame processing and held-button edges remain. Native event/polled trigger latches are unchanged, so this is not a return to double-clicking or trigger leakage.

Five fixtures exercise the production dispatcher with controlled time, including a direct command50ms after a menu click, preserved pointer suppression, missing-controller/hidden-session/loading rejection and the existing Start path. All359 local model tests pass. The native sequence retains its immediate Back-to-B interaction; no extra test wait or automatic retry was added to hide the defect.

The game is version0.12.2 and its XR component has the same revision. Rotunda0.12.1 is unchanged. App code changes only its version label; difficulty, enemy behavior, health pickups, core scoring, graphics, soundtrack, saved records and the0.35s performance safeguard remain unchanged. Existing legacy XR verification updates its expected module revision without changing interaction requirements.

The existing read-only workflow now saves the primary AR/VR report immediately after that suite as well as in its final full artifact, so interrupted or long companion checks cannot hide early evidence. No new workflow, branch, pull request or publication helper is introduced. Inspect the new exact source/public results before calling this native fix accepted. The separate quarter-resolution desktop performance failures remain open; physical Quest/Xbox/touch and owner balance/readability approval remain unverified.
