# Separate source retry / keep the remaining issue visible

Run 35641244480's public job remains successful: 96 exact served-file matches and all 105 public checks, including the complete Rotunda journey. The separately requested unchanged source retry did NOT clear its broader UI suite. Source object, water and interruption suites passed again, but the broader replay stopped after 30 checks at the AR music-adjustment assertion.

Downloaded source-retry artifact 10659157440 has SHA-256 50d1218d953ebf3f25df30eb459aaad446921a126a1ff847df5dd627923f4b5a. Its snapshot shows a newly started Mothership battle rather than the intended unchanged paused encounter. No script or shader error was captured. This is a failed interaction test, not a pass to be excluded from the report.

The input producer computes its XR target once without waiting for the rising panel's progress to reach its final position, unlike the screen-click helper. It also still emits native trigger selection/release on fixed wall-time delays. Under a slow rendered sequence the panel can move underneath that precomputed ray, or a neutral release can go unobserved. The trace is consistent with the wrong control being activated; this has not been established as a physical Quest diagnosis or as a newly introduced water-runtime bug. The same unchanged script and runtime completed all public checks, so an unconditional pass/fail claim about real hardware would be unsupported.

Next test-hardening task: wait for the actually open, fully raised panel before computing an XR control's world point, ensure neutral/pressed/released native input is observed over frames, and check the resulting selection before sending the next action. Preserve the exact pause/progress/score equality assertions. Do not assign focus, page, actor, score, clock or health, invoke the game's actions directly, or catch and ignore a wrong chapter. If the input-consistent test still exposes a runtime problem, repair the real problem before expanding effects.

No further runtime or test change is hidden in this final documentation checkpoint. The reusable water and its native source/public suite are delivered; full-source UI repeatability and physical-device acceptance remain open. The first-pass public receipt, earlier failures and input scripts are all retained for the next coding session. Fire and tree modules remain subsequent work.
