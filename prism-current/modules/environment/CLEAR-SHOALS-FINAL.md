# Clear Shoals / final inspected checkpoint, 2026-09-23

Water Optics 0.2.0 and Water Detail 0.1.0 are implemented and published in the normal Prism game. The optical runtime is a3cd5dbac40d330371f78bccd842c91a061fc889, unchanged by subsequent test/documentation checkpoints. Pages run 35903331074 successfully deployed support commit 6e1e938e7a8f1e80eebcb911f6dc66848b8b1cc7. Both inspected dedicated public receipts matched all 25 loaded runtime/style/release/attribution files. Deployment is verified; repeatable complete public AR gameplay is NOT yet verified.

The visible upgrade consists of a more detailed procedural pebble bed, refracted-light caustic patterns, fine moving wave normals, filtered distant highlights and dielectric reflection. Base Water 0.1.0 still supplies the same geometry, CPU height query, wakes, splashes, tide and pausable clock. The existing saved opacity and quiet controls remain; the AR room is not replaced by a solid sky or an opaque standalone ocean demo. Opacity is per surface and can compound at overlapping wave silhouettes, not a guaranteed whole-frame maximum.

The reusable optics uses two generated mipmapped RGBA8 data textures and no additional scene-rendering pass, float render targets, separate renderer or passthrough image sampling. Caustics are baked once at a reference depth and advected, not recalculated from each geometric wave. Clearwater is the raw WebGL2 inspiration, not an imported Three.js plugin. CLEARWATER-NOTICE.txt retains its MIT license for the adapted Fresnel helper; the spectrum, flux bake, pebble data, reversible attachment and tests are newly authored here. CLEAR-SHOALS.md documents APIs and limitations.

## Successful source journey and preserved failed repeat

The first corrected dedicated source run, 35901892136 at 146796cd1a6f26dcc6ed953424a2ce16a722c04e, passed all 26 native optical and AR checks, plus 19 actual bundled-Three resource checks. It completed the actual Easy Duck Armada battle through tracked input, with 12 laser hits and boss damage, 18 damage and healing, and five pickups. It preserved paused health, points, time and selected colors during sound/opacity changes and exited XR normally. It tested right-hand color switching but did not perform matching fruit cuts; those are separate chromatic tests. The local model/data suite passed 431 tests.

That run's public counterpart matched all files and passed 18 checks, then paused at 1.042154 seconds with no enemies present. This failed public journey was not hidden behind the source success. One individual retry request was rejected because the parent workflow was running. A subsequent support-only save produced one actual unchanged-runtime/test repeat in run 35903334050. That repeat failed in both source and public and has now been inspected. No additional green-seeking rerun is claimed or scheduled.

The source repeat, job 107325234106, passed 17 preceding checks and then timed out waiting for a wake because the encounter had paused at 0.8999319728 seconds. The captured frame interval was 352.4ms. The preceding JavaScript render submission was 0.9ms and tick 0.6ms; about 350.9ms was outside those measured callbacks. Its archive 10770555223 was downloaded and verified as SHA-256 0c69d2b2e626ea42edfd0059cb72793257fb570d35b35ba82a60a06af21a90fb.

The public repeat, job 107325234285, matched all 25 files and passed 18 preceding checks, then paused at 1.1901814059 seconds. The captured frame interval was 547.1ms; preceding JavaScript render submission was 1.1ms and tick 0.4ms, with about 545.6ms outside those callbacks. Its archive 10770595178 was downloaded and verified as SHA-256 cb8dbb7930fafdfc3bc285746dd8ab03a20a402618b4b90d3b85d7633b5e2560.

Both repeats had health 100, no actors or attacks yet, and the existing rendering-stall message. Both passed all isolated optical pixel/alpha checks and the preceding actual AR opacity, quiet, health and independent-color controls. Neither captured JavaScript or shader errors. Short JavaScript render submission is NOT completed GPU time and does not establish that the unmeasured interval was unrelated to rendering. The cause remains unresolved; do not label it a shader regression, a harmless CI artifact, or a physical Quest result without further evidence.

## Exact visual and preservation evidence

The actual 960x640 optical comparison was inspected, not generated as a concept image. It contains 306270 visible water pixels; 235407 differ substantially in color from the old shader on unchanged geometry. The source stereo AR capture was also inspected, at the existing reduced test framebuffer size. This is native renderer evidence, not physical passthrough imagery, normal-resolution sustained performance, or proof of equivalence to Clearwater's demo.

The earlier new alpha test incorrectly treated per-fragment opacity as a global cap. Native measurements found the same 64 above-cap oblique overlap pixels in both base and upgraded water and none overhead. The corrected test requires zero same-geometry composed-bound violations and zero newly above-cap locations, plus strict overhead/quiet caps and zero-opacity transparency. All repeats return those same successful optical measurements. The original failure and diagnostics remain in CLEAR-SHOALS-ALPHA-RESULT.md; no production geometry or quality change was used to satisfy the corrected contract.

A baseline comparison examined 63 original JavaScript/MJS/HTML/CSS/SVG runtime files. Only index.html, water-optics.js and water-optics.mjs changed; the other 60 are byte-identical. The index change loads the new helper and cache version. The helper and attribution are additions. Color Match 0.13.0, enlarged Easy fruit badges, score ledgers, health, healing, difficulties, finale bosses, AR islands/clouds/grass, Field Guide, audio and all retained gameplay remain intact. Source and public retry archives contain the same runtime and test hashes as the successful source journey.

## Resume here

Publication, one completed source AR battle and isolated optical correctness are established. Repeated full public AR acceptance, broader legacy suites, physical Quest appearance/comfort and sustained performance remain open. The final save changes evidence only and does not rerun tests or alter the played code. Read CLEAR-SHOALS-VALIDATION.json for machine-readable receipts, CLEAR-SHOALS.md for reuse, and the main FUTURE-DIRECTION.md for the campaign/library plan.

The next task is to reproduce and attribute the startup frame gap across the entire frame, including GPU completion and asynchronous work where measurable, without assigning game state, automatically resuming a failed test, lowering the required actions or weakening the 0.35-second safeguard. Compare the same scene and resolution with optics enabled/disabled in an explicitly labeled diagnostic, not by secretly shipping a reduced experience. Keep failed traces. Get owner Quest feedback on riverbed detail, target visibility and opacity before adding further expensive effects. Save directly to freshly reconciled master; no PR, branch, private hub, portals, sibling-game edits or progress resets.
