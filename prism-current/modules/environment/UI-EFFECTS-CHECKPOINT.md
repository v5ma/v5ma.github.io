# Currentworks spatial-UI reference pass / saved checkpoint

First direct-master save: af9f00a15f343989a55359bd7ec63a66f498fb6a. The support checkpoint adds the reference/licensing review, FLEX-SURFACE.md, real Three object/ray tests, a separately labeled WebGL fixture, accurate module registry and durable FUTURE-DIRECTION.md updates. No new PR, branch or workflow is created; the existing read-only source verifier now records the standalone surface fixture before its unchanged game replays.

FlexSurface0.1.0 is an original reusable geometry module with classic/ES entry points. It supports a bounded cylindrical bend and localized pull, a pinned attachment edge, correct normals/bounds and a readable back using reversed faces and mirrored UVs. The exact same deformed triangles determine picking. It takes the host's Three.js and FrontSide material without owning a renderer, DOM, input capture, clock or game state. It is not cloth physics, HTML-in-Canvas, a copied Saurow example or a new scene visible in Prism. index.html does not load it.

Local models:405 pass (387 recovered plus12 surface tests and6 expanded registry cases). Actual bundled Three r184 resource/ray tests:25 pass, including front/back intersections after nonuniform parent transforms, invalid input preservation, no redundant buffer updates and cleanup. Local WebGL2 is unavailable. The actual native fixture must separately prove pointer-selected quadrants agree with rendered pixel colors before any GPU success claim. That fixture is not a game screenshot or a physical Quest test.

The current recovered gameplay is Friendly Current0.12.2 / AR Tide0.1.1, already containing two islands, clouds and tiny grass patches. The registry no longer falsely describes these as unintegrated or omits grass/islands/water-optics. No loaded runtime, difficulty, health/healing, boss timing, soundtrack, control or save code changes in this pass. Existing failures remain open; FUTURE-DIRECTION.md records the exact source/public baseline artifacts, not a blanket live success.

REFERENCE-REVIEW-SPATIAL-UI.md identifies the first short link as r3f-webgpu-perf and records its estimated20/80 CPU/GPU split. It distinguishes real timing from inferred metrics, the HTML library's MIT license and actual XR example bridge, the native WICG proposal, known CSS/text limitations, TSL SSGI cost/history needs, unestablished glass redistribution permission and unavailable Saurow/CodeSandbox/CodePen implementations. No third-party packages, private hub assets or combined playtest brief were imported.

Next: inspect this exact source's native surface artifact. Prototype a noncritical HTML-backed settings/help surface and explicit per-source gesture ownership separately, while preserving the existing numeric health and Start/Resume paths. Full-screen GI, multi-pass transmission, dynamic curved primary menus and true cloth simulation remain separate experiments, not implicit upgrades. AR clarity, current Easy gameplay, reliable session recovery and the player's physical feedback remain primary.
