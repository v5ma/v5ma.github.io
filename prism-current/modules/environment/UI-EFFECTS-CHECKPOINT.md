# Currentworks flexible-surface/source-review checkpoint

The latest requested reference pass covers r3f-webgpu-perf, a folding-mesh article/demo, SSGI, gesture handling, Liquid Fabric, WebGPU transmission and three-html-render. Exact source/licensing review and integration priorities will be saved with the next support checkpoint. References are not blanket production or headset guarantees.

This first durable save adds original FlexSurface0.1.0 and its explicit ES facade plus twelve pure/data tests. It is NOT loaded by Prism index.html. It bends actual bounded CPU geometry, preserves the left attachment edge, supports a small localized pull, updates differentiated normals/bounds and uses the exact same triangles for picking. Its optional readable back has reversed winding/normals and mirrored UVs, not an independent invisible hit rectangle. It borrows caller materials without disposing them and owns no renderer, DOM, pointer capture, clock or game state.

Local full models:399 pass (387 recovered plus12 new). Actual bundled-Three object/ray tests:25 pass, including front/back picking after nonuniform parent transforms, stable buffers, rejected shapes and disposal. Those object tests and a separately labeled native fixture follow in the support checkpoint. Local WebGL2 remains unavailable; no rendered/GPU or physical Quest claim is made at this save.

Recovered game baseline is760e6f0d06adf7858c41f6a0c691f7c1034d1e85, with AR Tide0.1.1 and Grass0.1.0 already integrated, not missing work. All loaded game source remains unchanged by these new files. The latest baseline source workflow failed and the public replay did not finish; retain its real reports rather than claim the new module fixes unrelated stalls. New HTML, glass or SSGI code is not silently installed over the current menu.

Save direct to freshly reconciled master. No PR, staging branch, new workflow, private hub, full player brief or sibling-game changes. Future notes will stay in ../../FUTURE-DIRECTION.md and this folder. Source compatibility and successful object tests are separate from gameplay integration and physical validation.
