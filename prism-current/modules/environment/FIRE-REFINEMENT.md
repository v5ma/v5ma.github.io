# Fire 0.1.1 / refinement and loading checkpoint

The first integrated Fire 0.1.0 build at 0f516f422858f8a8b8cc93884ca273098697c742 passed its complete PUBLIC verification in run 35646541093: fire 19, water 13, interruption 19 and Rotunda 73 checks, with matching published bytes. The public artifact 10659949885 has SHA-256 71757000bb54980e14c684d18ed4476e9e8575cc5907e0cb40b509c202b6a3eb. Its five isolated shader fixtures and actual-game paused-fire image are separate evidence, not interchangeable gameplay screenshots. The public reports were downloaded and inspected.

Its source job passed models, both object suites, water, interruption and the full Rotunda journey. The dedicated fire browser test failed waiting for a pause after its first destruction. Artifact 10661600124, SHA-256 18b728bfa45a0e06d8499af1bacd4b3167f552dbce8c1155c7a44c573a5d1b0d, preserves the no-script/no-shader-error trace. The snapshot has the render-stall message but phase playing. The production P key toggles pause/resume, so a prior automatic stall-pause can make that press resume instead of pause. This is consistent with the trace, not a timed GPU profile proving the precise driver cause. No stalled run is relabeled as a pass.

Fire 0.1.1 explicitly uploads its density texture through the existing renderer's initTexture before gameplay, then compiles with the host scene. The actual start method awaits this preparation inside the existing serial/visibility cancellation guards, before playing audio. The host application is versioned 0.11.2 and cache URLs are updated together. It does not change the 0.35-second stall guard, skip the first destruction, lower test resolution, force a pause, assign a score or bypass any assertion. The native test now also explicitly asserts completed preparation and no stall pause at first destruction.

Preparation shares concurrent requests, does not expose hidden slots while compiling, propagates failures, permits explicit retry and cannot mark a disposed module ready. Four tests invoke the actual start method to verify audio ordering, cancellation, visibility loss and a newer operation. Six real-Three asynchronous ownership checks cover upload, compilation, visibility and cleanup. These are controlled collaborator tests, not physical-device claims.

The new warm orange outer flame and concentrated hot core replace the overly pale first palette. Density, ray count, particle caps, damage and gameplay are unchanged. Five updated configurations were rendered using the actual bundled Three.js WebGL2 renderer under software Mesa at 960x640, with no captured script or shader errors. The burst and jet images were visually inspected. This is a standalone shader fixture, not a live-game or headset screenshot.

All 288 Node tests and 29 real-Three fire object/async checks pass locally. Existing water tests remain required. The updated source/public results must be read from this checkpoint's actual workflow; no future pass is assumed. The module owns no extra renderer, render target, input or game loop. Core combat, XR/Rotunda, water, soundtrack and saved formats remain intact. The only app change is waiting for prepared graphics before audio inside the existing safe loading sequence.

References: official Three.js WebGLRenderer documentation distinguishes compileAsync from initTexture, noting texture uploads can cause first-render lag. The same documentation describes targetScene for shader preparation. https://threejs.org/docs/pages/WebGLRenderer.html

Next: inspect the updated source/public tests and screenshots, record the result, and continue to the independent Trees module in the next user-requested pass. The earlier failure stays in this record regardless of subsequent success.
