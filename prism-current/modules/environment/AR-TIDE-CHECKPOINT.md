# AR Tide / integration checkpoint

Standalone original Islands0.1.0 and Water Optics0.1.0 were saved as f6d20da988ca8207ca0e829c0b34f359616a3e21. This second checkpoint connects the actual Friendly Current AR game: two compact islands with one existing tree each, two Cloudlets groups and the previously saved Toon factory for island lighting. The old full banks remain hidden in AR; the new intentional composition is separate. Mothership keeps its current space scene.

Use the actual Rotunda Controls tab, then AR scenery. Islands + clouds is the default; Minimal scenery hides added decoration without changing enemies, health, clock, difficulty or saved records. The selection is saved with layout preferences and survives Reset panel. The semantic control alternative has the same choice. Existing water opacity, quiet, recentering and direct B/Y remain. The loaded Rotunda component is0.12.3; the unchanged host/core remains0.12.2.

Water Optics keeps the existing surface simulation and CPU query. It refines macro/detail modulation, broadens filtered sun highlights, shades foam rather than treating it as a bright outline and adds a cyan thin-crest term. An AR-only near-viewer alpha fade further reveals the room without changing saved opacity. There is no FFT, screen-space reflection, sampled passthrough, second renderer or added persistent render target.

The new modules are reusable, caller-owned-engine components. The small Prism adapter owns the combined scene, preparing its bounded GPU resources in the existing cancellable loading path before music. It never moves the headset camera or writes gameplay state. Scenery is hidden when very close to the viewer, not used as a movement boundary or furniture detector. Exact projected sightlines and physical comfort still require playtesting.

Local integrated tests:376 Node model/lifecycle/integration checks and23 actual bundled-Three object observations pass. Local WebGL2 is unavailable. Native source/public tests are added at the front of the EXISTING read-only verifier, with immediate checkpoint artifacts, while retaining every older test path. They require actual rendered AR entry, menu scenery/opacity selection, pause, damage/healing, a tracked block cut, a complete Easy late-boss battle, chapter switch, exit and saved preference reload. No native/public pass is claimed before the resulting artifacts are inspected.

The source research inspected Abyssal Ocean's README, actual r180 index and MIT license. It supplies concepts, not copied code or a new renderer dependency. Its full-screen opaque FFT/SSR/underwater environment is not substituted for the room-visible AR game. The Liquid Glass URL exposed a loading shell only; no unseen implementation is claimed. The exact links and API/validation notes follow in the closeout.

Preserve core gameplay, old saves, soundtrack, health, four difficulties and late bosses, plus current private/public boundaries. The next step is to inspect the actual shader, menu and AR battle artifacts, fix revealing failures, review real renders, then update the registry/README/checklist and closeout. No PR, branch, new workflow, private hub or other-game edits are introduced.
