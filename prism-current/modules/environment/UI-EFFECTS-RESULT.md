# FlexSurface reference pass / inspected native result

Read this result alongside UI-EFFECTS-CHECKPOINT.md, FLEX-SURFACE.md and REFERENCE-REVIEW-SPATIAL-UI.md. Source af9f00a15f343989a55359bd7ec63a66f498fb6a and support commit 8db868572caa80d8561beefab9f2e41f71814953 are saved directly on master. FlexSurface remains a library-only implementation; no loaded Prism game script changes in this pass.

The reusable-surface step in source run 35808550266 succeeded. Artifact 10728357667 was downloaded and SHA-256 checked against 4acca5a35711d3a80ec1427bb5c30c3f467840012b5c55733e691559ea069b78. Its object report passes 25 checks; its native report passes 21 checks with no captured script or shader errors. The actual front and back screenshots were inspected.

The native fixture used a 960x640 software-rendered WebGL2 context. Ordinary pointer events selected all four colored quadrants on both sides of the curved, locally pulled panel. Picked UVs and framebuffer pixel colors agreed. The same deformed geometry, not a separate flat hitbox, provided those intersections. Repeating a paused shape requested no geometry updates; reset returned the same buffers to a flat surface. The back's numbers read normally rather than as a mirrored image.

The local complete suite passes 405 tests. The source workflow's model/integration step also succeeded. These facts do not establish that the new geometry is integrated into the Rotunda, HTML content is supported, controller/hand gestures are complete, or a physical Quest meets a frame budget. No new scene has been silently substituted for the actual game. The independent AR Tide and public-file/game checks were still running at this checkpoint; previous source pauses and the historical public-file mismatch remain open in FUTURE-DIRECTION.md.

The final save adds only this receipt and UI-EFFECTS-VALIDATION.json. It intentionally does not modify files already listed in the running publication manifest or supersede the running native game checks. No production code, workflow, geometry setting, assertion, resolution, difficulty, health, soundtrack, camera, input or save is changed by this receipt. Its documentation-only commit skips triggering duplicate CI; the code and tests have already run on the named source.

The library now has entries for water, fire, trees, toon, cloudlets, islands, water optics, grass and FlexSurface. The registry distinguishes integrated current modules from this library-only addition and retains historical verification separately. Other games can use an explicit adapter and the caller's Three.js rather than import Prism's gameplay.

Next development should prototype a noncritical spatial panel or an isolated HTML-backed settings/help surface. Rendering HTML, routing a controller ray, handling text focus, preserving keyboard accessibility and preventing menu input from also firing are separate obligations. Keep the existing Start/Resume and numeric-health paths until a replacement has been proven. SSGI and multi-pass glass remain separately licensed and backend-tested experiments; a WebGPU screenshot is not permission or compatibility evidence.

All future decisions and new outcomes continue in ../../FUTURE-DIRECTION.md. Keep the exact module limits, inaccessible-source notes and existing failures visible across chats. No PR, staging branch, new workflow, private hub material, full multi-game brief, or sibling-game modification is introduced.
