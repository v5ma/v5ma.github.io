# Pass 4 / connected shoreline and renderer diagnostics

The first durable fix is e79ac9bdcf4d2e9c67b4d1be497e1b67309f662e: host0.11.3 no longer resets the canvas drawing buffer for every blade/menu sync. Real quality changes retain their original ratios. The 0.35-second stall safeguard and all collision/scoring/input/audio rules are unchanged. This follow-up supplies explicit negative/repaired native tests, bounded frame diagnostics and a visible shore/sky integration pass.

The previous straight block banks and tall cone reeds are replaced by sloped terrain, 24 irregular stones and 80 narrow geometric reed leaves. The authored bankHeight function drives both visible ground and the water module's bed samples. Wet shading follows the actual tide. The eight existing trees sit on the matching dry plateau; seeds, shapes, horizontal positions, detail levels and wind are unchanged. Geometry remains outside the central action corridor. The visible sky and shoreline reuse the existing water data texture instead of allocating new textures or a reflection pass; sky color/cloud breakup now agrees with the water's analytic sky approximation. This is not a real scene reflection or a fluid simulation. AR and Mothership keep the shore and forest hidden.

Shore geometry totals7536 triangles, down from the former block/rock/cone assemblage, but uses three draws instead of one and a more detailed surface shader. That is not an unmeasured claim of faster Quest rendering. Existing water/fire/tree module source is unchanged; integration is in RiverArt and bank-trees. The sun and new terrain are scenery, not colliders or new attacks.

The test-only frame tracer records bounded CPU tick, art/menu, render submission, input, pause, resize and long-task observations. It creates no input, advances no simulation time and changes no renderer settings. CPU submission is not GPU completion. The negative test substitutes only the former sync code and checks four redundant resets from four real preference actions. The fixed path requires zero unchanged-ratio resets through preference changes, blade swaps, start and pause, plus real slicing/destruction and unchanged state. Genuine quality changes must still occur. Existing full-battle and XR journeys remain independently required.

Local checks before this checkpoint: all322 Node tests pass; actual bundled-Three water37, fire64, trees83 and new coherence19 object checks pass. Controlled object checks are not native GPU or physical-device tests. Exact source/public outcomes and rendered screenshots must be inspected from the existing read-only verifier before closing this pass. No new workflow, PR, branch, private hub, portal or sibling-game edit.
