# Native opacity result and corrected test contract

The diagnostic run35901190226 at2e7c35d9 retained the failed original whole-frame alpha assertion. Its source artifact10768898169 was downloaded and SHA-256 verified as8b6b5016547d221113c16fd27ac75f36ac467e148c682d5d20ae53425bf01539. Both the original Water0.1.0 and the new optical layer reached alpha255 at exactly64 pixels in the oblique view. The most opaque pixel was(529,425) in both. The new image covered306270 pixels, of which235407 differed substantially in RGB from the old water. No shader/script error occurred.

The renderer's clear alpha was0 and the actual water uniform was0.8. DoubleSide with forceSinglePass was active as intended. Switching only the isolated fixture to FrontSide still produced62 over-bound pixels and maximum254. The overhead view had zero over-bound pixels and maximum178. The phenomenon is projected surface overlap near the far wave silhouettes, not an extra optical render pass or an overwritten opacity uniform. It already exists in base water. No runtime material-side change is adopted.

The new test had incorrectly treated a per-fragment opacity as a whole-frame cap. Source-over compositing compounds overlapping fragments as1-product(1-alpha). The corrected test renders the exact same moving triangles with a constant0.8 alpha and requires every actual optical pixel to remain within that geometry's composited upper bound. It also requires no additional above-cap locations compared with base water, and retains a strict0.8 cap in the nonoverlapping overhead view. The zero-opacity, rotated quiet-AR0.38, paused-image and genuine AR-control tests remain. This corrects the tested contract instead of changing game geometry or reducing quality to satisfy the original mistaken assertion.

Important limitation: the opacity control is not a guarantee that no overlapping distant water pixels can ever become more opaque than the single-surface setting. The existing near-viewer fade remains, and no solid sky or room-image sampling is introduced. Preserve this finding in future API claims; a future true whole-frame opacity layer would require its own compositing and performance design. This checkpoint changes tests and this evidence only. Actual game AR acceptance still must complete.
