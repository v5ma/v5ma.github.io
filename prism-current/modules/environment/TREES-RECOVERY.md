# Trees first native run / preserved failures and loading repair

The integrated checkpoint is 80e991f16b5be4e527bf0ea0df090f9e5309c95c. Run 35659639581 matched all 111 public files. Source artifact 10667192409 (SHA-256 5de2e1fc17eb858712385f7d62ddb66d128f2bcbbd1e088fdc715d57713b2017) and public artifact 10667007683 (SHA-256 d2b5a3a8c1d2286b70050dbe1912fe02486e1ac75b57f351a83b66ec5c37e3f4) were downloaded and inspected.

Both source and public completed the full 73-check Rotunda journey, including both normal Arcade battles. The actual main-entry screenshot shows the bank-side palms and broadleaf trees while retaining the existing menu and central view. Both jobs compiled the new tree shaders successfully, and no captured script or shader errors explain the failed independent suites. Source305 models and tree38/fire64/water37 object checks passed. Public fire24 passed; source fire stopped at a startup rendering pause. The new tree suite and water suite stopped at a rendering pause around 21 seconds, and the interruption suite completed AR then paused during initial VR playback. These failures remain failures; the passing broader journey does not erase them.

Trees 0.1.2 adds explicit preparation before the soundtrack: the existing host renderer compiles and draws every prebuilt tree detail mesh into one disposable 24x24 target, with display/linear output policy matched to the current destination. Cloned draw objects preserve the actual geometry/material buffers but never move or reveal the host forest. Target, viewport, scissor, XR routing and auto-clear state are restored. The target is disposed immediately, and no per-frame render pass is added. No time, input, score or game state is advanced by preparation.

This is an intervention against first-use geometry cost, not a claim that all observed stalls have already been causally diagnosed. The actual native tests must demonstrate whether it resolves them. The original 0.35-second game safeguard, resolutions, effect budgets, slicing/laser checks and full-battle criteria are unchanged. A bounded observer records slow renderer calls if the new tree suite fails again; it only reads timing/state and does not invoke game actions or change outcomes.

All 305 local Node tests pass with the new preparation; 75 actual-Three tree/object/preparation checks pass, including explicitly controlled renderer collaborators. Fire64 and water37 object checks also pass. The collaborators verify state restoration, all-LOD participation, failure/retry, shared asynchronous work and safe disposal; they are not GPU or physical Quest evidence. Source/public validation of this exact new checkpoint remains separate.

The original tree descriptors, geometry, normal/wind fields, detail levels and riverbank placement are unchanged from 0.1.1. Water, fire, original art/app/XR/Rotunda, music, combat and saved progress remain intact. The adapter chains the existing fire preparation before tree preparation. No PR, staging branch, additional workflow, private hub source, portal or sibling-game update is introduced.
