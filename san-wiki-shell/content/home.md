# SAN Wiki Reader Shell

This public package contains the SAN Wiki reader shell and its shared reader runtime only.

SAN articles, source documents, graph data, research history, and other SAN content are intentionally omitted. The empty data contract keeps the reader route testable without publishing the SAN knowledge base.
