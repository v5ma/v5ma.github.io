# Tidegate Crossing continuation

Current replacement candidate: tidegate.html, build tidegate-20260915.1. Read TIDEGATE-CROSSING.md, AAA-ROADMAP.md and design/PLACE-MASTERY-SOURCE.md before changing geometry or progression. The retained source essay is the user's basis, not evidence that the proposed approach has passed human playtesting. Sky Cycle and all sibling games remain outside this task.

The ordinary purpose is a working outpost, pump house and supply crossing. Four descriptions must remain coherent: physical routes, conditional access, animal behavior and information cues. The orange home crane and amber station mast anchor orientation. Preserve useful observation, service/interior, boat and helicopter approaches, actual herd clearance, reversible sluice safety and the lasting shortcut. Do not replace these relationships with more randomly scattered props or obligatory itinerary triggers.

Tidegate has its own save namespace and layout identity. Keep Classic Reserve at index.html; do not erase its progression or load its coordinates into this layout. The initial replacement candidate is opt-in while human acceptance remains open. The new report is one-time district completion, not Classic Reserve credits. Parked vehicle positions are not persisted yet; safe foot checkpoints and mechanism state are.

DioramaXR extends, rather than rewrites, ReserveXR. First-person VR, third-person VR and explicit immersive-ar miniature views are separate options. There are exactly three front/top opening states and never a sealed box. Only rendering scales the world, with original coordinates restored in finally. No real table detection, persistent spatial anchors or room scanning is claimed. Keep current hand/controller neutral, release, session and visibility safeguards.

The first native run exposed a missing species ID in the audio adapter, now repaired without changing the Classic audio engine. Real Rapier traversal also found a roof-ramp lip, now corrected. The model suite has 157 passing tests in the local candidate, including actual boat crossing and helicopter landing/disembark. Native browser, legacy and public evidence must come from successful runs of the final source; pending checks must not be described as passed.

Run all Node tests, JavaScript syntax checks and tests/tidegate-browser.py. The main native tour walks via the real synthetic-Xbox input and collision system with no teleports or forced mission flags. Its XR portion uses explicit API/pose mocks. tests/tidegate-view-browser.py separately captures the actual miniature renderer with documented mock eye poses; these are not headset or passthrough images. Retain all four Classic browser journeys. The Tidegate workflow archives exact source and model results, then gates the new candidate release on successful live-byte matching and published-route acceptance.

Remaining human gates: unfamiliar-player wayfinding, recognition of alternatives and the return connection, replay benefit, art/readability, physical Xbox, Quest 3 controllers/hands, real stereo/AR compositing, comfort and frame-time measurement. Do not mark those complete based on automation. Replace the campaign entry only after acceptance, preserving a recoverable Classic expedition.
