{
  "name": "dino-atlas",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Dino Atlas Classic Reserve and Tidegate, with preserved expeditions, vehicles, field operations and XR.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check-source.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
