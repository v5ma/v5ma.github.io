# Dino Atlas Field Rotunda recovery

Branch dino-atlas/rotunda-release-20260920 starts at master919a7cdebfb3a9935c7f4f983b724ecac0d92067. Intended build ranger-spatial-console-20260920.3 and prerelease dino-spatial-console-20260920.3. Read ROTUNDA-RECOVERY.md and verification/spatial-console/rotunda-recovery-20260920.json. Do not repeat PR206 or the .2 merge2a527853cffebc837f078dafdc015124b629992d. The previous spatial-refinement branch is retired; its bookkeeping job is not acceptance evidence.

The source archive from run35555017066 was hash-checked and all72 manifest hashes matched. Its Classic and Tidegate spatial journeys both failed a synthetic wrist ray following otherwise successful movement/workspace/Resume checks. The ray approached the non-rendered underside of a single-sided slate. Correct only the device-pose fixture; keep production picking unchanged. A real CSS defect in the same screenshots left the closed settings dialog visible. The .3 runtime adds one narrowly scoped closed-dialog visibility rule and the browser checks the actual layout at launch, Resume and XR exit.

The .3 local suite passes257 model/physics tests without failures or skips. Three new fixtures retain backside rejection, exercise front-side information/buttons and hidden ancestors, and assert the scoped CSS contract. Native/source and public completion are pending at this checkpoint. Use actual workflow/artifact results, not this plan, for acceptance. No actor, mission, inventory or reward assignment manufactures native results.

The scoped workflow applies the hash-checked Dino-only source once, removes transport, commits it and records its exact SHA. Do not merge staged transport. Candidate checks cover all models, both spatial journeys, both portal/Express journeys, regular-game entry and original hand controls. Refresh master before normal expected-head merge; never force or replace sibling work. Pages deployment, served-byte matching and public gameplay are separate checks. Archive the release only after public success without replacing historical tags.

Preserve the full reserve and Tidegate, all existing saves/rewards, missions, mounted tools, Express travel, controller profiles and character-centered portal size/placement. Keep the screen DOM fallback. No private hub source/review/combined transcript, credentials or assets are included; no engine migration or new travel portals. Physical Xbox/Quest, wrist orientation/readability, actual stereo/passthrough, comfort and device performance remain open. The next work should follow concrete hardware feedback and complete the existing on-foot/vehicle/mission experience.
