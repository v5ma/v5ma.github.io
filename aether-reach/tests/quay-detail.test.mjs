import {test} from 'node:test';import assert from 'node:assert/strict';import * as T from '../vendor/three.module.js';import {polishEquipment,recolorFacade} from '../quay-detail.mjs';
test('Equipment bevels stay within the original unit-box envelope and do not alter world meshes',()=>{const camera=new T.PerspectiveCamera(),old=new T.BoxGeometry(1,1,1),m=new T.MeshStandardMaterial(),first=new T.Mesh(old,m),second=new T.Mesh(old,m),world=new T.Mesh(old,m);camera.add(first,second);polishEquipment(camera);assert.equal(first.geometry,second.geometry);assert.notEqual(first.geometry,old);assert.equal(world.geometry,old);first.geometry.computeBoundingBox();for(const axis of ['x','y','z']){assert.ok(first.geometry.boundingBox.min[axis]>=-.50001);assert.ok(first.geometry.boundingBox.max[axis]<=.50001);}assert.equal(first.material,m);});
test('Pale facade treatment preserves shared textures and never changes the red-brick source material',()=>{const source=new T.MeshStandardMaterial(),texture=new T.Texture();source.name='MI_Trim_Green';source.map=texture;const a=new T.Mesh(new T.BoxGeometry(),source),b=a.clone(),c=a.clone();recolorFacade(a,'stone');recolorFacade(b,'stone');recolorFacade(c,'brick');assert.notEqual(a.material,source);assert.equal(a.material,b.material);assert.equal(a.material.map,texture);assert.equal(c.material,source);assert.equal(source.name,'MI_Trim_Green');const shader={fragmentShader:'#include <map_fragment>'};a.material.onBeforeCompile(shader);assert.match(shader.fragmentShader,/quayLuma/);});
