# Field Focus 0.17.1

This is an observation-led guidance repair within the existing story and Currentworks city, mapped to X12, V03 and R03/R04. It is not another district, new locomotion or a replacement interface. The reviewed master was 7518b8e007bb0debdd474f8ff25fbfa41dd3d69f. Its complete Aether subtree and native workflow matched the accepted 5c34a62624c103a179ed7c94dddb2fd5fbb5df3a release. Preserve newer sibling commits when integrating.

## Player changes

The existing objective reminder (default Xbox D-pad Up) now names the actual focused destination, its direction relative to the courier, distance and elevation. It no longer promises a projected route that does not exist, nor substitutes a generic relay instruction for the tracked adventure. The five-second reminder uses the same destination as the gold beacon, atlas, floor guide and journal. In XR its notice follows the existing two-second floor-message/history path. Direction is relative to the character, not the viewer's head or a free diorama camera. Keyboard N retains its existing Field rig action; M opens the atlas. The old duplicate N navigation branch is pre-empted by that rig action, so this release does not claim a working keyboard-N reminder or silently remap it.

Desktop What do I do? uses E for keyboard and the actual saved use/reload binding for an active Xbox-compatible controller. Touch-controller XR retains Right grip; a standard controller inside XR uses its saved binding. Merely plugging in an unused controller does not replace keyboard prompts. Mixed X / E labels are normalized before showing a remapped mechanism action. The prefix parser requires an actual key separator, so ordinary names such as East gallery and Elevator landing do not lose their first letter. Ladder prompts with E / are also normalized.

A completed saved task, especially Bellwether's separate reward path, no longer traps the destination marker at a finished desk. Presentation suggests the first unfinished task in the existing journal order. An unfinished task deliberately chosen by the player is never overridden. The atlas no longer draws its older second diamond from the raw saved task; its single shared marker uses the same focused destination as the beacon and guide. The journal marks a fallback as NEXT SUGGESTED and the story reader names it; this does not rewrite the saved tracked ID, invent completion, or repay credits. All-adventures-complete removes the stale marker and leaves free exploration and the separate Silent Network expedition available.

Once the courier is on Bellwether's 12/17/22-meter maintenance galleries, the destination follows the existing authored upper walkway points toward the receiver instead of sending them back down to the street ladder. This includes the gallery recovery during the receiver encounter. The hint stays on the final stair landing and roof connector until they are cleared; switching straight to the receiver at 26 meters led a guidance-following fixture into the wall beside that landing. The original ground-level south ladder remains a supported alternative; receiver, circuit prerequisites, enemies, six-second hold and reward are unchanged. This is a local authored route hint, not a navmesh or a promise that arbitrary off-route bearings are walkable.

## Evidence and boundaries

Twenty-six new isolated tests cover eight rotated bearing sectors, vertical targets, every completed task, all-complete exploration, read-only saves/events, keyboard/pad/XR bindings, mixed-prefix handling and the original walking gallery. The gallery fixture advances through ordinary model movement with the starter kit; initial fixture placement is not a browser gameplay completion claim. The first prefix test caught East becoming ast; the fixed delimiter rule passes the same assertion without weakening it. Four additional tests follow only the actual suggested target at 0.05, 0.1, 0.2 and 0.5-second decision intervals, in both ascent and receiver-recovery stages. The original experiment stopped at the landing wall; the correction reaches receiver range without rescue, new progress or reward. These are isolated model fixtures, not a completed hostile browser mission.

The full local suite passes 386 Node tests. Existing native controller and field-guide journeys add actual-input checks for keyboard E, Xbox X and the correct nonmodal objective reminder, retaining all old story, combat, remap, movement, graphics and save assertions. Local Chromium navigation returned ERR_BLOCKED_BY_ADMINISTRATOR; this is recorded as blocked, not passed or bypassed. The existing seven-suite native, hosted-file and source-backup workflows must validate the new exact commit. The release receipt, rather than this design document, establishes their final outcomes.

All Currentworks snapshots and graphics remain unchanged. Preserve story chapters, version-1 saves, equipment/rewards, controller mappings and rearming, current-step diorama shooting, full-depth aperture, floor rotunda, hand menus, original tracked first-person VR and every sibling project. No new menu, input binding, engine, render pass or workflow is introduced. Physical Quest/Xbox appearance, comfort,performance and unfamiliar-player comprehension remain unverified.

Next: play the actual arrival-to-Bellwether loop with the revised cues. Inspect small/large window legibility and controller-remapped use text before adding more content. Remaining desktop HUD crowding and longer-term character/dialogue work are not claimed fixed by this pass. Roll back only these Aether paths with a forward commit; never reset master or clear browser storage.

## Recovery and startup repair 0.17.2

Recovery found the final 0.17.1 implementation at 42b90d0e0e4c96157a9b99dd2f76f6122239e606. Its unchanged controller journey passed on retry: 46 checks, including the theatre ladder, earned equipment, Continue and neutral reconnect. The seven source suites total 281 checks. All 143 public files matched. Keep the first controller artifact 10737751333 from run 35829344157: after 37 completed checks it timed out at ladder distance 18.15, y25.15, with gameplay still running and no rescue. The identical retry artifact is 10789054676. Two isolated model fixtures reach the roof in 7.1833 seconds from the ladder base and 1.675 seconds from the recorded timeout pose; they are not browser completion or proof of the timing cause.

The later actual-HTTPS field-guide run 35953165157 exposed a different real defect. Artifact 10790000548 (SHA-256 ec253ea8ea2e83bc5eb67dc1f5273127bf37f02d3774e70f5f4e6650f997ef22) completed 52 checks, including story, spatial sizing and Exit XR, then reloaded. Startup failed to expose AetherReach within the original 120-second timeout. The page reported Cannot read properties of undefined (reading 'isReady').

Aether's Currentworks preparer called renderer.compileAsync on the entire city. Its r177 renderer retains a material set and subsequently polls each material's currentProgram. Independently, the existing HDR sky loader disposes and replaces the old sky material. A disposal between collection and a readiness poll removes those program properties. The asynchronous timer exception is outside the preparer's promise rejection path, leaving startup waiting. This is a source-identified race consistent with the observed exception, not a reason to disable the sky or hide the browser error.

Version 0.17.2 narrows both asynchronous and synchronous preparation to the owned Currentworks group. The third targetScene argument still supplies the actual city lights and environment. All rendering, scenery placement, quality, gameplay, controller, story, save and shader APIs remain unchanged. No vendored library or renderer is patched.

Two controlled r177 lifecycle fixtures reproduce the old material-set ownership failure and fail against the old adapter. Both pass with the scoped preparation. They explicitly simulate program-property removal after a host sky replacement, and separately cover synchronous fallback. The real-GPU fixture adds owned-scope and real material-replacement checks while retaining all existing tree warmup, water/cloud draw, equal-scale stereo aperture and cleanup assertions. The ordinary full-game reload/Continue journey remains required and is not shortened or weakened.

The 0.17.1 permanent release remains intact. Do not confuse its successful source/byte/backup checks with this subsequently discovered live reload regression. New 0.17.2 native, hosted-byte and archive results require their own receipts. Local Chromium navigation remains blocked by administrator policy; no local browser pass is claimed. The new full local model suite contains 388 tests.
