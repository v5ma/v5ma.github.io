[
{"path":"aether-reach/app.mjs","before":"f8d49fa3d9106ee1cb9337e0d842ca48e4ee75210698f1093ee64c15a12118a8","after":"c05bb453f69e1f0cfd3341277d75b3b95ab0212929f739d71f5f14e81e880e22","edits":[
[0,0,"import {ROOFS,LADDERS} from './rooftop-world.mjs';\nimport {createModalHost} from './ui-navigation.mjs';\nimport {cleanControllerProfile} from './controller-profile.mjs';\n"],
[16,16,"settings.controller=cleanControllerProfile(savedSettings?.controller);\n"],
[20,21,"const modal=createModalHost({beforeOpen(){paused=true;clearInput();unlock();audio?.suspend().catch(()=>{});},afterClose(){if(playing&&!state.won){paused=false;accumulator=0;clearInput();canvas.focus({preventScroll:true});}}});\nconst dialogs=()=>modal.dialogs();\n"],
[27,31,"function showDialog(id){modal.show(id);}\nfunction closeDialogs(){modal.closeAll();}\nfunction pause(){if(!playing||dialogs().some(d=>d.open)||state.won)return;showDialog('pause-dialog');}\n"],
[34,35,"function action(name){if(!playing||paused||state.won)return;if(expeditionUI?.action(name)){events();return;}if(tactics?.action(name)){events();return;}if(combat?.action(name))return;if(name==='interact'){if(!interact(state))toast(nearby(state)?.label||'Look for a rail station, a relay console, or an archive tablet.',2);}if(name==='jump'){if(devices?.xr.active&&!state.p.grounded&&!state.p.rail&&!state.p.climb)toggleGlide(state);else jump(state);}if(name==='glide'&&!toggleGlide(state))toast('Jump or release a rail first. The foldwing needs clearance and charge.',3);if(name==='pulse')pulse(state);if(name==='reverse')reverseRail(state);if(name==='reload')reloadQueued=true;if(name==='fire')fire(state,devices?.aim);if(view)events();}\n"],
[36,37,"function events(){for(const e of state.events.splice(0)){eventHistory.push({type:e.type,time:Number(state.time.toFixed(2)),...e});if(eventHistory.length>250)eventHistory.shift();view.effect(e);combat?.effect(e);tactics?.effect(e);expeditionUI?.effect(e);devices?.effect(e);sound(e.type);if(e.type==='glide-open')toast('FOLDWING OPEN · steer with movement keys/stick; S brakes; G/B folds. Looking alone does not steer.',5);if(e.type==='glide-fold'&&e.reason==='empty')toast('Foldwing spent. Land or catch a rail to recharge.',4);\n"],
[44,45," if(e.type==='win'){paused=true;clearInput();unlock();$('run-stats').replaceChildren();for(const text of [`${state.records.size} / ${RECORDS.length} archives`,`${state.stats.rails} rail rides`,`${Math.round(state.stats.railDistance)} m by rail`,`${state.stats.defeated} hostiles defeated`]){const span=document.createElement('span');span.textContent=text;$('run-stats').append(span);}showDialog('complete-dialog');}\n"],
[60,60," for(const r of ROOFS){g.strokeStyle='#d6acfb';g.lineWidth=2;g.strokeRect(X(r.x-r.w/2),Z(r.z-r.d/2),r.w*scale,r.d*scale);g.fillStyle='#efd6ff';g.font='bold 11px Arial';g.fillText('ROOF '+r.y+'m',X(r.x),Z(r.z));}for(const r of LADDERS){const p=r.points[0];g.fillStyle='#f5c97b';g.fillText('H',X(p[0]),Z(p[2]));}\n"],
[67,68,"function newExpedition(){const saved=store.get(SAVE);const begin=()=>{store.remove(SAVE);start(false);};if(saved)modal.confirm('Start a new expedition?','Your saved checkpoint, equipment, credits and adventure progress on this device will be replaced. Cancel keeps all of it.','Start new expedition',begin);else begin();}\n$('start').onclick=newExpedition;$('continue').onclick=()=>start(true);$('settings-button').onclick=()=>showDialog('settings-dialog');$('pause-settings').onclick=()=>showDialog('settings-dialog');$('pause-button').onclick=pause;$('resume').onclick=()=>{$('pause-dialog').close();};$('return-title').onclick=title;$('map-button').onclick=()=>{map();showDialog('map-dialog');};$('explore-more').onclick=()=>{$('complete-dialog').close();state.won=false;paused=false;};$('new-expedition').onclick=newExpedition;\n"],
[71,72," if(dialogs().some(d=>d.open)){if(e.code==='Escape'){e.preventDefault();const d=modal.top();if(d.id==='complete-dialog')return;d.close();}return;}\n"],
[80,81,"document.addEventListener('pointerlockchange',()=>{lookReadyAt=performance.now()+180;if(document.pointerLockElement!==canvas&&playing&&!paused&&!dialogs().some(d=>d.open))pause();});document.addEventListener('pointerlockerror',()=>{if(playing)toast('Drag to look or use the arrow keys. Mouse capture is unavailable.',4);});\n"],
[88,88,"let frozenFrame='';\n"],
[92,93," combat?.update(elapsed);tactics?.update();expeditionUI?.update();devices?.updateHints();\n // Paused desktop menus keep a static scene; controller polling and DOM UI remain live.\n const frozenKey=playing&&paused&&!devices?.xr.active?[innerWidth,innerHeight,view.camera.fov,state.p.weapon,state.tactics?.power,settings.reduced,view.stats().visual.requested].join('|'):'';\n if(frozenKey&&frozenKey===frozenFrame)return;frozenFrame=frozenKey;\n"],
[95,97,"function installPauseDeck(){const root=$('pause-dialog');for(const[id,text,fn]of [['pause-journal','Adventure journal',()=>expeditionUI.open(true)],['pause-atlas','City atlas and archive',()=>{map();showDialog('map-dialog');}],['pause-loadout','Equipment and outfitter catalogue',()=>combat.open(true)],['pause-dismiss-notice','Dismiss current notice',()=>{toastUntil=0;$('toast').textContent='';}],['pause-checkpoint','Return to saved checkpoint',()=>modal.confirm('Return to your checkpoint?','Keep your earned credits, gear and completed adventures. An active escort or defense attempt will stop.','Return to checkpoint',()=>{closeDialogs();rescue(state);events();})]]){const b=document.createElement('button');b.id=id;b.textContent=text;b.onclick=fn;root.append(b);}}\ntry{view=makeView(canvas,matchMedia('(max-width:680px)').matches?'low':'balanced');view.camera.fov=settings.fov;resize();installVisualSettings(view);$('start').disabled=false;$('start').textContent='Enter the district  ↗';$('load-status').textContent='Lumen Circuit v0.6.0 ready - 12 districts, 6 rooftops, 15 rails, 13 adventures';updateContinue();combat=installCombatUI({state:()=>state,view,settings,playing:()=>playing,paused:()=>paused,xr:()=>devices?.xr.active,show:showDialog,toast,events,persist,focus:()=>canvas.focus({preventScroll:true})});devices=installControllers({show:showDialog,topDialog:()=>modal.top(),confirm:(...args)=>modal.confirm(...args),view,state:()=>state,settings,saveSettings:()=>store.set(SETTINGS,JSON.stringify(settings)),start:()=>{if(!playing)start(!!store.get(SAVE));else{closeDialogs();paused=false;}},clear:clearInput,pause,playing:()=>playing,paused:()=>paused,action,map:()=>{map();showDialog('map-dialog');},hint:()=>state.p.gliding?'FOLDWING '+Math.ceil(state.p.glideCharge)+'% · movement stick steers/brakes · A folds':nearby(state)?.label||$('toast').textContent});flight=installFlightUI({state:()=>state,view,playing:()=>playing,paused:()=>paused,xr:()=>devices?.xr.active,action});tactics=installTacticsUI({closeAll:closeDialogs,state:()=>state,playing:()=>playing,paused:()=>paused,show:showDialog,toast,events,action,aim:()=>devices?.aim,powerAim:()=>devices?.powerAim});expeditionUI=installExpeditionUI({state:()=>state,playing:()=>playing,paused:()=>paused,show:showDialog,toast,persist});installPauseDeck();view.renderer.setAnimationLoop(animate);\n window.AetherReach=Object.freeze({version:VERSION,snapshot:()=>({expedition:expeditionSnapshot(state),ride:state.p.ride?{id:state.p.ride.id}:null,climb:state.p.climb?{...state.p.climb}:null,grounded:state.p.grounded,tactics:JSON.parse(JSON.stringify(state.tactics)),devices:devices?.snapshot(),playing,paused,won:state.won,time:state.time,glider:{active:state.p.gliding,charge:state.p.glideCharge},position:{x:state.p.x,y:state.p.y,z:state.p.z,yaw:state.p.yaw,pitch:state.p.pitch},rail:state.p.rail?{...state.p.rail}:null,health:state.p.health,shield:state.p.shield,ammo:state.p.ammo,weapon:state.p.weapon,scoped:state.p.scoped,credits:state.kit.credits,owned:[...state.kit.owns],tune:JSON.parse(JSON.stringify(state.kit.tune)),picked:[...state.kit.taken],fov:view.camera.fov,reserves:{...state.kit.reserve},reload:state.p.reload,energy:state.p.energy,relays:[...state.relays],records:[...state.records],checkpoint:state.checkpoint,stats:{...state.stats},enemies:state.drones.map(b=>({id:b.id,kind:b.kind,humanoid:!!b.humanoid,wave:b.wave,walking:!!b.walking,heading:b.heading,telegraph:b.telegraph,x:b.x,y:b.y,z:b.z,hp:b.hp,stun:b.stun,shocked:b.shocked||0,burning:b.burning||0})),interaction:nearby(state)?.type||null,interactionId:nearby(state)?.id||null,target:(()=>{const t=railTarget(state);return t?{id:t.rail.id,name:t.rail.name,distance:t.distance,point:{...t.point}}:null})(),renderer:view.stats(),events:eventHistory.slice(-60)})});\n"]
]},
{"path":"aether-reach/climbing.mjs","before":null,"after":"a39652d9a74d69f531a400a7017d7d4da249c196878c11d51c6f07d1b5709c5b","content":"/* Continuous, reversible ladder motion; no teleport, save position or cheat API. */\nimport {LADDERS} from './rooftop-world.mjs';\nconst dist=(a,b)=>Math.hypot(a.x-b.x,a.y-b.y,a.z-b.z),pos=a=>({x:a[0],y:a[1],z:a[2]});\nexport function ladderLength(r){return r.points.slice(1).reduce((sum,p,i)=>sum+dist(pos(p),pos(r.points[i])),0);}\nexport function ladderPoint(r,d){let left=Math.max(0,Math.min(ladderLength(r),d));for(let i=1;i<r.points.length;i++){const a=pos(r.points[i-1]),b=pos(r.points[i]),length=dist(a,b);if(left<=length||i===r.points.length-1){const t=left/length;return{x:a.x+(b.x-a.x)*t,y:a.y+(b.y-a.y)*t,z:a.z+(b.z-a.z)*t};}left-=length;}}\nexport function createClimbing({occupied,clearLine,emit}){\n function nearby(s){const p=s.p;if(p.climb)return{type:'ladder',id:p.climb.id,label:'Climbing: forward/up ascends, back/down descends; jump or Foldwing button lets go'};if(!p.grounded||p.rail||p.ride)return null;\n  for(const r of LADDERS)for(const end of[0,1]){const q=pos(end?r.points.at(-1):r.points[0]);if(Math.abs(p.y-q.y)<.7&&dist(p,q)<2.1&&clearLine({...p,y:p.y+1},{...q,y:q.y+1},s))return{type:'ladder',id:r.id,end,label:'E / Climb '+r.name};}return null;}\n function leave(s,jump=false){const p=s.p;if(!p.climb)return false;p.climb=null;p.vx=p.vz=0;p.vy=jump?5.5:0;p.grounded=false;p.hookCooldown=.3;emit(s,'climb-leave');return true;}\n function enter(s){const n=nearby(s);if(!n)return false;if(s.p.climb)return leave(s);const r=LADDERS.find(r=>r.id===n.id),p=s.p;p.climb={id:r.id,d:n.end?ladderLength(r):0,approach:true};p.gliding=false;p.hookRequest=0;p.vx=p.vy=p.vz=0;p.grounded=false;emit(s,'climb-start',{id:r.id});return true;}\n function step(s,input,dt){const p=s.p,c=p.climb;if(!c)return false;const r=LADDERS.find(r=>r.id===c.id);if(!r){leave(s);return false;}\n  const length=ladderLength(r),axis=Number.isFinite(input.moveZ)?Math.max(-1,Math.min(1,input.moveZ)):(input.forward?1:0)-(input.back?1:0);let q;\n  if(c.approach){const target=ladderPoint(r,c.d),l=dist(p,target),f=l?Math.min(1,dt*4/l):1;q={x:p.x+(target.x-p.x)*f,y:p.y+(target.y-p.y)*f,z:p.z+(target.z-p.z)*f};if(l<.02)c.approach=false;}\n  else{const next=Math.max(0,Math.min(length,c.d+axis*3.3*dt));q=ladderPoint(r,next);if(!occupied(q.x,q.y,q.z,s))c.d=next;}\n  if(!occupied(q.x,q.y,q.z,s)){Object.assign(p,q);p.vx=p.vy=p.vz=0;p.grounded=false;}\n  else{leave(s);return true;}\n  if(!c.approach&&((c.d===length&&axis>.1)||(c.d===0&&axis<-.1)))leave(s);\n  return true;\n }\n return{nearby,enter,leave,step};\n}\n"}
]
