# Freefield recovery / September 17

Public baseline is verified Freight Cut 0.15.0. The earlier uncommitted Freefield implementation did not survive the session; it was not a published release. Recovery uses the actual Freight Cut source archive and the pinned asset-review archive from workflow 35274641958. Do not repeat the claim that all earlier local changes remain available.

This checkpoint preserves the newly recovered preference, input-remap, collision-checked blink and native-control modules. They are not wired into public master yet. A scoped integration script and new unit tests are under local review. All 365 local model/source tests passed after wiring these modules locally, including 3600 simulated seconds of actual running. The first duration-test corridor met a real shelter wall; its revised open-ground course measures speed away from turns, without moving the actor by test assignment. This is a model fixture, not an hour in a physical headset.

New preferences are separate from checkpoints. Free Stride defaults to 9 m/s and no running stamina drain; legacy movement remains selectable. Player footsteps default off, water movement 15 percent, and the score becomes sparse. Enemy hearing/gameplay sounds remain authoritative. Menu controls are reserved; saved remaps affect gameplay only. Blink follows clear swept collision ground and cannot cross locked walls or deep water. No completed browser or release claim is made.

Next recover the real perspective portal from the reviewed Dino Atlas source (not the old six-plane crop); remove always-visible first-person panels; implement direct Quest controls with usable hands and safe menu recovery; integrate the licensed local weapon files. Then test the Meridian redesign, progression and saves rather than calling a geometry sketch finished. The source and asset reference is only a reference, not permission to overwrite sibling games. All work remains in this Rainward branch until exact-source native validation and public-byte verification succeed.
