import {test} from 'node:test';import assert from 'node:assert/strict';import * as T from '../vendor/three.module.js';import {enableDioramaClipping} from '../diorama-shaders.mjs';
test('Original custom water and particle shaders clip their deformed positions without replacing uniforms',()=>{const scene=new T.Scene(),uniforms={time:{value:4}},mat=new T.ShaderMaterial({uniforms,vertexShader:'void main(){vec3 p=position;p.x+=.2;gl_Position=projectionMatrix*modelViewMatrix*vec4(p,1.);}',fragmentShader:'void main(){gl_FragColor=vec4(1.);}'});scene.add(new T.Mesh(new T.PlaneGeometry(),mat));assert.equal(enableDioramaClipping(scene),1);const once=mat.vertexShader;assert.match(once,/vec4\(p,1.0\)/);assert.match(mat.fragmentShader,/clipping_planes_fragment/);assert.equal(mat.uniforms,uniforms);assert.equal(mat.clipping,true);assert.equal(enableDioramaClipping(scene),1);assert.equal(mat.vertexShader,once);scene.children[0].geometry.dispose();mat.dispose();});
