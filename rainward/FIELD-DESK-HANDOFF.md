# Rainward Field Desk / saved release candidate

Continue the published XR Repair v0.16.1 rather than a replacement demonstration. Baseline master: d539c8e6bdf16339df16d3c514df1a2f4043af15. The staged Field Desk source was integrated in 215f5fe465a607c4e1e7b2f3e7df7065c6f09acf. The hash transport and write-enabled staging workflow are removed from the final candidate. Use the read-only field-desk workflow plus existing compatibility and actual-compositor journeys.

The desk captures a local reference when summoned and remains there through head turns and menu selections. Height, distance, scale and yaw are bounded and saved separately from progress. Its optional virtual pedestal is calibrated relative to the summon pose, not to a detected physical floor. Reduced motion is the default; closing removes the panel hit target immediately. Hold B / R3 and open-left-palm recovery remain. Resume, map, satchel, acquired clues and music controls are prioritized on the first page.

The additional review fixes a hidden-reading input conflict: placement no longer advertises an active reading target, so focused A adjusts the desk instead of dismissing the underlying clue. Closing placement restores the entire reading. The XR map heading names its real next objective. Reset restores the layout but preserves pedestal visibility and motion preferences. Acquired clues are retained separately from transient feedback and can be restored from existing earned checkpoints; unread solutions are never granted.

All 417 local model/source tests passed after this review. The staged source passed 414 tests in run 35555987987, artifact 10620640298, archive SHA256 a341ba113eaae641d3cfe3a1952509eafa76a39954c2193a5627fa181a5fd501. Its native jobs are earlier-source evidence only. Current-source native tests must exercise transformed rays, controller focus, hand menus, reading/placement, audio, save preservation, both-eye images, actual XR shutdown and reentry. Local HTTP navigation is administrator-blocked; no bypass was attempted.

Preserve seven expeditions, progress, remaps, rewards, characters, licensed files, default fast movement and the v0.16.1 muzzle/scope/compositor fixes. No private WebXR SaaS code, full studio brief, hub configuration or review file is copied here. No A-Frame migration, walking/sphere portals, free-hand panel dragging or unrelated content expansion is included. Desktop native UI remains available; this pass improves the XR spatial interface.

Publication is not established by this document or a version string. Require exact-source acceptance, a normal merge preserving concurrent sibling work, Pages delivery and an independent served-file comparison. Keep failed evidence and record what was actually tested. Physical Quest/Xbox, real passthrough appearance, player comfort, visual quality and sustained hardware performance remain separate unapproved gates.
