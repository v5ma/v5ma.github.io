# Rainward 0.16.1 XR repair / final-source checkpoint

Recovery source: cd6f88bb06fc663269d94c0698bd5df1197563e0 on fix/rainward-xr-recovery-20260918. The final source removes temporary integration scripts and the write-enabled candidate workflow. The permanent compositor acceptance workflow is read-only. Do not revive old preparation scripts. Public status must be read from the repair PR and subsequent served-byte receipt; this checkpoint alone is not publication evidence.

The repair includes reserved hold-B menu recovery, full acquired puzzle/field-note text, transient feedback, mechanical firearms aligned with the pointing ray, authorized tracked-muzzle shots, chapter-local scope reset, default fast land running, one-pass AR/portal compositing and a safe uninitialized-portal guard. All 399 local model/source checks passed, including the hour-long running fixture. Local HTTP browser navigation is administrator-blocked; no bypass was used. Repository CI supplies native Chromium/WebGL.

The older exact-source run 35408099286 passed both compositor paths for AR Diorama and VR Diorama, while first-person cases exposed faulty synthetic navigation/defense. The corrected driver targets the actual type-specific torso and steers once per XR frame with braking. It may fire and reload through actual tracked inputs while defending against live pursuers. No actor, health, ammunition, enemy, clock, clue or checkpoint assignment manufactures acceptance. Retain that distinction from physical Quest or human reaction evidence.

Run 35410744265 checks the saved corrected source. The final PR must also run all existing compatibility tests and both external-render-target paths for each of the four XR views. Read both success reports and failure captures; the new tests read compositor attachments rather than the desktop canvas and deliberately tilt grip axes away from target-ray axes.

Preserve seven chapters, save formats/keys, rewards, remaps, licensed files and concurrent sibling work. Inspect current master scoped changes before merge. After successful acceptance, merge normally, wait for Pages and independently compare public file hashes. Save the receipt and final reports here or in the PR. Actual Quest/Xbox, real passthrough appearance, hand reliability, comfort and sustained hardware performance remain unapproved until device feedback confirms them.
