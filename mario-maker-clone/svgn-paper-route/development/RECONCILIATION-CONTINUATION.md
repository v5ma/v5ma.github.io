# Sky Cycle reconciliation continuation, September 21, 2026

The owner requires direct master commits. Do not create another PR, staging branch or temporary workflow. Preserve sibling applications, current XR repairs, official route identities, original physics, saves, music and user-created documents. This continuation starts from the published v0.27.1 runtime fda53bedb376d54c3bad3bdacf5a15aba85dba61 and keeps its recovered Canal Choice geometry.

## Actual release evidence and newly exposed failures

Run 35565728678 passed the independent 38-file public match at 2026-09-21T06:50:17Z, plus public AR entry, VR entry and lower-canal replay. It also passed the eight fork scenarios, all-button and Workspace AR/VR, Portal Network, twelve-delivery and Bathhouse checks. It did not pass everything: the rules job reached 406 successful game rules and then failed loading the original soundtrack test; Sunrise failed at its original current-view card click before entering gameplay. Do not report the whole run as green.

The root package now declares type module for the wiki tooling. The old CommonJS soundtrack test's require of browser UMD adventure-score.js no longer returns its expected API. The repair is game-local: tests/original-score.test.cjs evaluates the unchanged browser source in a classic-script VM scope and retains all twelve original assertions. Neither the shared package nor the production score is modified. The consolidated workflow now retains its source archive, including the actual parent package mode, even on a test failure, while still returning the failure status.

The Sunrise log identifies Route journal in the header intercepting the ordinary route-card click. Its retained video shows clipped/overlapping card text and presentation buttons. The route-entry wrapper let its main button shrink inside a bounded, centered nested grid. The fix gives cards intrinsic row/content height, keeps modes from shrinking into text, and uses the outer chooser as the scroll owner. The actual old card click is retained; no forced click, UI bypass or player-state assignment is used. A separate native test checks all 32 visible route actions across four viewport sizes, then starts the original Sunrise card, rides normally and uses the Screen choice. Source CSS guards are supplementary, not rendered acceptance.

The local combined pass has 410 game rules and all 12 original soundtrack checks. Local browser navigation was blocked by administrator policy before game load and was not bypassed. Exact-source hosted and independent public results for v0.27.2 remain required. The existing Sky Cycle workflow is extended, not replaced by another deployment pipeline.

## Older PR51: preserve Cloudpost, defer the distinct ordered passport

The old Sky Post branch ba04ea67b70e4cd6ce64a8f1b056e957270698d3 proposes the peg peg-124-18 at (4482,666), a balcony called sky-post, and a m5-to-balcony-to-m8 route. Current sky-relay.js already uses that peg with a newer tuned cloudpost-relay curve, its own accepted-finish handling and existing integration. Do not install the old route wrapper: it would duplicate the peg and overwrite later builders, metadata and input fixes. The old live-registry and modal keyboard integration ideas are already represented by current owners.

The six ordered-route passport stamps are distinct from the current district/individual-rail journal. They are not claimed implemented merely because a journal exists. Retain this unported design as SC-REC-PASSPORT: adapt ordered catches, the actual release/underside events and renamed current relay to the existing journal after every offered route has a current ordinary-input witness. Preserve the prior branch and its svgn.route-passport.v1 contract as provenance; do not add a second unreviewed runtime or reset any existing ledger. Closing the obsolete wholesale PR does not complete this feature.

## Older PR27: keep current Ride Lab, rewrite rather than restore the old solver

Branch 9fbe34ecb92f9f485cf6bc375c82e26785713d84 contains a global motion-state graph, generated layouts, automatic receiver proposals, a second planning board and source-materialization machinery. Current Ride Lab already provides a private current grip-engine trace, explicit scenario inspection, stale-result protection and undoable joins. These are not equivalent to a complete global graph or automatic Fit catcher.

The old solver's actor/visited-state model does not distinguish the current two-sided rail contact state and its initial jump assumptions are fixed. Its historical 30/34/38 generated layouts and old campaign/editor wrappers are therefore rejected as current playable replacements. Retain the useful unported solver goal as SC-REC-FIT: use the current private RailGripCore, actual document/face/grip state, swept terrain/deck clearance, protected trajectories, bounded evidence and one undoable proposal before native acceptance. Preserve original reports and the 26-task planning source in branch history; the current canonical AAA roadmap remains authoritative. Do not restore transport writers or duplicate workflows.

## Older PR19: preserve exploration concepts without replacing current campaign slots

Branch 2dbd9b0286189044620075c18672990bbd66b686 contains an editor predating the recovered current Workshop and two layout prototypes. Its builder routes every index from 4 onward through make(i-4) for only two new specifications; applied wholesale to the current eight routes, it would replace existing chapters and cannot serve all their indices. Reject that registration and the obsolete editor files.

Switchback Quarry and Relay Vault remain unported concepts, not hidden completed chapters. Retain SC-REC-EXPLORE: adapt them first as optional, non-awarding Workshop documents with unique stable identities, current metadata, complete traversable recovery, key/door state and real finish/delivery tests. Quarry's open-ramp descent must not replace the approachable opening road; Vault's key-return/door relationship needs actual current collision and controller/XR verification. No automatic campaign promotion or award migration is authorized by an audit.

## Next work and boundaries

The prior c946 Canal Choice useful receiver/signs were recovered directly and PR176 closed with its incompatible remainder explained. Preserve that shipped work and the original all-button/AR aperture/session cleanup. The next content opportunity remains a useful optional high-to-high whip continuation in Waterwheel, with non-whip recovery. The three SC-REC items above retain genuinely missing older functionality without pretending a conflict resolution finished those designs.

No private hub, rotunda, cross-game portal or A-Frame migration is part of this repair. Physical Quest/Xbox, headset comfort, advanced-editor ergonomics, full XR chapters and long-session performance remain separate open checks.
