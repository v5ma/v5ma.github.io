# Verbatim historical handoffs

These are byte-preserved snapshots of documents that originally lived one directory above, in `development/`. Resolve their historical relative file references from that original directory. They are retained for provenance and integration details, not as the current release queue.

Read `../RESUME-HERE.md` and the latest versioned verification receipt for the active source, acceptance, publication and next content gate. Earlier current-version headings inside these snapshots do not override those records.
