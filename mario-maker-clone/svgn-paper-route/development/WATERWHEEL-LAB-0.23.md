# Waterwheel Lab v0.23.0

Build: `sky-cycle-waterwheel-lab-2026.09.15`. Continue the approved chapter-design work inside the existing Sky Cycle. `verification/waterwheel-lab-0.23.json` is authoritative for exact accepted source, native checks and publication. A prepared candidate is not a live release.

## Delivered implementation

The supplied drafts are integrated in `LEVEL-DESIGN-METHODOLOGY.md`. `chapters/WATERWHEEL-BOULEVARD-R2.md` contains the concrete specification and promotion workbook; `chapters/design-source-provenance.json` records both supplied file hashes and their roles. The complete ground adventure and ambitious connected optional sky network remain the design promise. Chapter quality takes priority over further cosmetic or destination series.

This slice is a labeled, editable Workshop preview, not a ninth campaign destination or silent replacement of the old Waterwheel layout. All eight existing campaign builders, route identities, physics, direct controls, audio and save owners remain. The Workshop isolates playtest settlement: preview completion awards no campaign medal, badge, stamp, daily result, ghost record or persistent credit.

Seven sections connect South Quay Post Office, Parcel Market, Service Bridge, Express Junction, Millworkers Court, Waterwheel Galleries and Wheelhouse Depot. There are twelve road delivery targets, five checkpoints, two terraced rises, four separated shield-supported patrol encounters, a wheelhouse landmark and nine signs drawn in 2D and 3D. Target presence does not certify every delivery or human enjoyment.

The short Parcel porch returns to the road before the express entrance. The upper ride connects `ww-runway`, `ww-crescent`, `ww-gallery` and `ww-finish` through unchanged two-sided rail physics. Two lower recovery surfaces remain only initial authoring. Five editable transfer intentions name approaches, receivers, recovery intervals and onward states. A dedicated visual annotation editor is not implemented.

## Play, editing and saves

Open Routes and choose **Waterwheel r2 design preview**. Select **Ride the new layout**, **Ride with the sky hidden**, **Open editable blueprint**, or **Restore previous blueprint**. The same entry is in Workshop / Worlds and drafts. Sky-hidden builds actually remove the aerial collision rails while preserving the identical ground journey.

Dirty drafts and active playtests block replacement. The validated prior non-preview blueprint must be saved to `svgn.skycycle.waterwheel-preview-backup.v1` before entry; failed backup writes block entry. Restore is explicit. Existing libraries and save namespaces are never cleared. Independent Bezier editing remains available, with advanced manipulation still pointer-required. The future `canal-choices-r2` record identity is reserved, not an active award migration.

## Controller and XR

Existing Xbox action mappings and remaps remain; jump, boost, paper, whip and interaction do not gain menu steps. The preview uses existing focus/confirm/back navigation. Seated XR retains controller rays and native hand-select actions.

The stereo UI fix puts opaque panel pixels in a late transparent render group, excludes fog/depth interference and puts pointers in a later group. Preview dialogue now measures the riding HUD and positions the original toast above it without suppressing warnings. Leaving the preview restores the original positioning owner; page-hide/restoration releases and reconnects observers.

## Acceptance and retained evidence

Native acceptance must separately finish the road beneath the sky, the ground-only build, the short returning porch and the continuous express. Browser tests inspect real 3D scenery, then use supported 2D for CPU-runner full-route completions. Old-save seeds are explicitly fixtures; trace wrappers read the real tick and never assign position, velocity, score or wins. Physical Xbox/Quest, full 3D hardware routes and human enjoyment are different gates.

The Waterwheel XR suite uses the real Three XRManager and stereo framebuffer with deterministic tracking hardware. It samples six real rendered DOM controls in each eye, verifies text/background contrast, and separately hides/restores only scene visibility to test that scenery cannot overwrite sampled menu pixels. This render-only fixture does not change physics or progression. It is not a complete XR chapter finish.

The original receiver ended at x=8710 and overshot the depot in the model. Its authored endpoint was moved to x=8550; declared carried-state cases then landed before the goal. This does not qualify every possible recovery.

The initial XR screenshot exposed scenery overwriting menu panels; explicit overlay groups corrected the runtime defect. The first later pixel oracle incorrectly equated raw canvas sRGB bytes to the renderer's transformed output and assumed a fixed optional-control order. Preserve run `35033434378`, job `104597072306`, artifact `10422039636`. A second oracle wrongly assumed Materials & FX must always be the third first-page control; run `35033815372`, artifact `10422278403`, captured Portal atlas there instead. Both are real asynchronously registered controls. The revised test requires six actual DOM-backed controls including all five invariant actions, retaining all 24 background samples, 12 text-region checks and scenery-isolation checks. Neither failure is relabeled as a pass.

An earlier South Quay screenshot showed dialogue covering the riding instructions. Preview-only measured positioning fixes that visible fault and retains original notification contents. Its earlier capture and source identity remain part of recovery evidence.

The older `sky-cycle/waterwheel-reference-0.23` default-replacement experiment is not the publication candidate. Do not merge it over this newer preview or Quiet Water. Future default promotion requires a deliberate, tested revision-aware record policy, not a second accidental v0.23 release.

## Next chapter work

Continue Waterwheel before further destinations: speed/brake-selected fork, useful lower discovery route, optional high-to-high whip connection with a non-whip alternative, swept collision/recovery coverage, all intended deliveries, retries, slower/coasting approaches and the historical optional canal sequence. Implement and rollback-test revision-aware settlement/display before replacing the default chapter.

Keep unfamiliar-player review, physical Xbox and Quest controllers/hands, real tracking loss, listening, comfort, native GPU frame times, XR layers/multiview and advanced Bezier controller authoring open. Publishing this preview completes a concrete development slice, not the entire approved redesign or AAA roadmap.
