The ac4fb14423fa67b8068a4c2bd51eeb93b5ef1f3b HTML transfer accidentally replaced established element identifiers and stylesheet links. Native boot checks caught the resulting null onclick failure before release. This source restores the exact known index blob, then applies only verified textual edits to that original markup. Two CPU tests now enforce the retained DOM bindings, unique identifiers and all nine local stylesheet references. This failure did not change the published game.

Actual gallery captures also showed a label blocking the apprentice. The gallery sign is moved behind the objective and reduced in size. Ordinary readouts use the retained screen-size limits and fade from the focal sightline when obstructing the apprentice. Stereo miniature and first-person views retain the authored sign geometry. The existing camera-wall correction remains, and all 325 local CPU/model/adapter checks pass. Native rendering and full release acceptance must rerun against the final ordinary source; earlier successes and failures remain diagnostic evidence only.
