# Way In / Neighborhood Missions polish

This pass implements the entrance-and-elevation guidance part of existing LEVEL-01 and XR-02. Gold remains the current task. A cyan WAY IN marker, compass direction and dashed map line point toward an existing public entrance or supported floor transition. The compact floor/controller HUD explains stairs and dismounting or the need to dock. No new map area, mission prerequisite, reward, locomotion assist or save identity is added.

The route uses the actual authored floors, stair slopes, wall clearance, canal water state and blue-door state. Opening the return shortcut changes the suggested route; it does not require replaying a long detour. The normal final objective remains visible. Unknown or unsupported positions retain that objective with an explicit public-entrance hint instead of promising a made-up path. Advice can be ignored: it does not steer, teleport, unlock or complete anything. It is a static walking suggestion, not an enemy/traffic avoidance system, and deliberately does not promise lift timing or moving-cart clearance.

A longer actual-input model run revealed a first-draft error that the initial approach tests missed: straight-line elevation interpolation redirected the type-case return at an overlapping arcade landing. The final guide uses stepped support and directed links. Five complete resident stories now finish through normal tick/action calls, with exactly-once rewards retained on reload. Model fixtures for water/gate alternatives are separate from those input-driven journeys. First build and query timings were measured only in the local Node runtime, not on Quest.

The old public main-navigation failure is preserved at run 35623737029, artifact 10652825563. Its state correctly held "No backup available", but its test read innerText from the intentionally hidden desktop toast. The driver now reads textContent, the source used by nativeMenuDescription; it does not remove the no-confirmation or error-feedback requirements.

The existing eight-mode console journey additionally selects the radio task, checks the stair approach, captures the actual map and verifies no task acceptance/credits were granted by selection. All previous console assertions remain. The shared import-map revision is wayfinding-1, and the public verifier checks all revisioned module URLs rather than hardcoding an older revision. The original archived files, control preferences, ledgers and sibling games remain unchanged.

Write completed changes directly to current master after reconciliation. No PR, staging branch or extra publisher. Confirm public hashes independently. Physical Quest/Xbox/hand reach, human readability and enjoyment, sustained hardware performance and full campaign acceptance remain distinct obligations. The next opportunity is richer physical entrance signage and resident reactions, after feedback on these route cues. Unported Signal City and indoor Tideglass features remain documented in INTEGRATION-AUDIT.md and are not claimed delivered here.
