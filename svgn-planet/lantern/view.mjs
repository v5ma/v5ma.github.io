// One canonical renderer for the main district and the preserved recovery entry.
// Omitting sharedRenderer creates the standalone renderer exactly as before.
export {createDistrictView as createView} from './district-view.mjs';
