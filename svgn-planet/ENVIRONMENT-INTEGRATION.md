# Currentworks / Neighborhood Missions integration

Owner request: reuse prism-current/modules/environment in Neighborhood Missions alongside the saved Highline direction. This file is a durable active checkpoint, not an implementation or rendering claim.

Inspected master: c31dd6c56a101aec7c8a890768ae1f845494b294. Neighborhood runtime is unchanged from 81fce8c1 / 9ed36aa7. Library directory tree is 86082336de4dc56c308267a914900a11e1e4f586. Local files are recovered from exact GitHub source artifacts and checked against library blob identities. The host is Three r177; the library targets r184. Do not replace the engine or assume compatibility. Test composed portal shaders, lifecycle, pausable clocks and stereo with the actual host.

Bounded goal: replace the flat Lantern canal water with pinned Water 0.1.0 plus Water Optics 0.1.0; replace primitive trees with Trees 0.1.3 at the existing roots; add small, bounded Cloudlets 0.1.0 formations; use Toon 0.1.0 selectively on new archive controls, not all character materials. Keep transparent AR, existing room-space UI, all eight views and independently adjustable portal size. No new renderer, external asset service, scene-copy pass, camera ownership, second Three version or mutable live sibling imports. Copy exact selected library blobs into a versioned game-local vendor area with attribution and hashes. Prism itself remains unchanged. Fire and island terrain are not required for this peaceful canal/roof composition and are not being silently advertised as integrated.

Planned gameplay companion: make the upper Print Exchange archive a connected usable room and add an optional follow-up to Sal's recording using the current validated campaign ledger. Preserve the original six Highline stages, all five earlier campaign cases, all old save identities and the 32 m altitude boundary. Move Highline entry into the first welcome screen so the new direction is discoverable without scrolling.

Evidence found before changes: public Highline job 106934175516 in run 35782836734 completed successfully. That does not close the full matrix: its legacy grounded and recovery-portal suites failed, and some jobs were cancelled. Keep those outcomes separate and do not claim all-green acceptance. New model, composed-material, rendered graphics, real-input story, publication and physical-device results must remain distinct.

Resume from current master plus FUTURE-DIRECTION.md, HIGHLINE.md, DEVELOPMENT-HANDOFF.md and the current evidence. Next task is implement the bounded adapter/room, test before atomic direct-master commit, inspect public bytes and rendered results, then update this file and the evidence receipt. Do not create a PR, staging branch, new publishing workflow or overwrite concurrent work.
