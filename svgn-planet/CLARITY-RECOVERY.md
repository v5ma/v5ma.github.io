# Neighborhood Missions mission clarity recovery

2026-09-20 Pacific. Continue from current master, not PR 185 or an earlier prototype. Recovered console runtime f1b0f198, merged in 2813ee6e; current master f25b6985 contains that exact game tree and newer sibling changes. All 346 baseline model/input tests pass locally. The eight source console jobs in run 35554765805 now pass. Public check 35554969890 did NOT pass: 262 of 281 files matched before its deadline, with 19 stale/missing updated files. Do not call that a successful deployment. Latest Pages retry was run 35558328704, attempt 2; recheck it and the latest master before publication.

This checkpoint adds eleven presentation regressions and the derived mission-card/status modules. Local integrated development passes 357 tests. The main-hub and native row rendering hooks are the next small commit. Mission previews use copied state; selection still uses the existing mission action. The HUD must read the current campaign tool, not the stale Watch tool, once the campaign kit is unlocked. Text changes never move geometry, rewrite real saved progress, grant rewards or remap controls.

Complete the native integration, extend the input-driven console test with real mission subtitles, run it through the ordinary main entry, merge normally, and verify actual served bytes separately. Preserve all old modes and controller/hand/exit checks. No private SaaS hub, external portals, framework migration or sibling-game edits. Physical Quest reach, readability and performance remain untested. Keep source/evidence checkpoints and record actual failed or partial checks honestly.
