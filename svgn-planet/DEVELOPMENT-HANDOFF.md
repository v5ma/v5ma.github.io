# Current pass: Way In

Read WAYFINDING.md and release.json. Entrance/elevation guidance now uses the existing geometry and changing routes, with cyan way-in and gold final-task markers. No gameplay ledger or original layout is changed. All local tests and actual source/public acceptance are recorded separately in the external receipt. Retain current direct-master instructions. The earlier reconciliation record below remains historical context.

# Neighborhood Missions / direct-master reconciliation

Read AGENTS.md, INTEGRATION-AUDIT.md, production/integration-audit-20260921.json and release.json first. Older instructions that require new pull requests, staging branches or source-import workflows are historical, not the current process. Write completed scoped changes directly to the latest reconciled master with a single parent and no force push.

The full main game remains index.html -> main-app.mjs -> main-hub.mjs / unified-xr.mjs. Lantern Ward is an integrated district. Keep all eight native first/third-person full-world/diorama AR/VR modes, current floor/controller console, mission subtitles and visible gamepad focus. Legacy entries are recovery routes, not the main experience.

This pass reconciles the useful physical left/right trigger option from the competing Field Desk into the current console. Existing preferences retain primary-hand driving until explicitly changed. On-foot, Courier and Xbox controls remain intact. All 102 original archive hashes and every independent mission/reward ledger remain protected.

Fourteen obsolete source/evidence writers or duplicate workflow wrappers are removed. Three existing read-only workflows retain the 19 source journeys, eight console modes and 21 public journeys. The public verifier also checks the actual entry, revisioned JavaScript URLs, MIME types and a final full second pass. No current gameplay assertion is dropped. npm test now runs the complete model/input suite instead of only its old tests subdirectory.

All 387 local checks passed. Five new checks use the real unified XR adapter with fake renderer/session/DOM; they do not render pixels. Local Chromium navigation returned ERR_BLOCKED_BY_ADMINISTRATOR. The extended browser journey must report its own result. The earlier Mission Focus source run passed, but its public job failed; do not call that a deployment.

Signal City's enterable cars, scout drone and Waterfront File, and Tideglass's indoor depth-swimming missions, remain distinct unported implementations requiring current-adapter rewrites. Keep their source branches intact; do not merge their old app/model/world replacements. Their disposition maps to existing DESIGN-02 and WATER-01 in the audit, without resetting the canonical roadmap or declaring human gates closed.

Check latest master again before every write. Preserve sibling games, saves, remaps, stable IDs and private/public boundaries. No private SaaS code, external travel portals or framework migration belongs in this repair. Record actual public outcomes and physical Quest/Xbox/hand, comfort and performance limitations separately. Store final receipts outside the game directory.
