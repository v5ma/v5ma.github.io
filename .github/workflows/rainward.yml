name: Rainward native survival acceptance
on:
  workflow_dispatch:
  pull_request:
    paths: ['rainward/**','.github/workflows/rainward.yml']
permissions:
  contents: read
concurrency:
  group: rainward-${{ github.ref }}
  cancel-in-progress: true
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 26
    strategy:
      fail-fast: false
      matrix:
        suite: [ui, mission, controller, controller-menus, improvements, expedition, visual, loot, guidance, station, scanned, cinematic]
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Model and public-boundary checks
        run: |
          for file in rainward/*.mjs; do node --check "$file"; done
          node --test rainward/tests/*.test.mjs
      - name: Install native browser
        run: |
          python -m pip install playwright==1.57.0 Pillow==11.3.0
          python -m playwright install --with-deps chromium
      - name: Play the actual HTTP game
        env:
          RAINWARD_SUITE: ${{ matrix.suite }}
          RUINS_SUITE: ${{ matrix.suite }}
          TERMINUS_SUITE: ${{ matrix.suite }}
        run: |
          python -m http.server 4173 --bind 127.0.0.1 >/tmp/rainward-http.log 2>&1 &
          PID=$!
          trap 'kill $PID' EXIT
          case "$RAINWARD_SUITE" in
            cinematic) FILE=cinematic.py ;;
            scanned) FILE=scanned.py ;;
            controller) FILE=controller.py ;;
            controller-menus) FILE=controller-menus.py ;;
            improvements) FILE=improvements.py ;;
            expedition|visual) FILE=conservatory.py ;;
            loot) FILE=loot.py ;;
            guidance|station) FILE=terminus.py ;;
            *) FILE=browser.py ;;
          esac
          timeout 1380 python -u "rainward/tests/$FILE"
      - name: Preserve exact source and test evidence
        if: always()
        run: |
          mkdir -p test-output
          python rainward/tests/release.py
          git rev-parse HEAD > test-output/tested-commit.txt
          git diff --stat > test-output/uncommitted-diff.txt
          tar -czf test-output/rainward-source.tgz rainward index.html projects.css
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: rainward-${{ matrix.suite }}
          path: test-output/
          retention-days: 7
