name: Rainward Clear Water candidate
on:
  push:
    branches: [feat/rainward-clear-water-20260913]
    paths: ['rainward/**', '.github/workflows/rainward-clear-water-prepare.yml']
permissions:
  contents: write
concurrency:
  group: rainward-clear-water-candidate
  cancel-in-progress: false
jobs:
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    outputs:
      sha: ${{ steps.source.outputs.sha }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/rainward-clear-water-20260913
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Integrate scoped patch and keep model regressions
        run: |
          python rainward/tools/prepare-clear-water.py
          python rainward/tools/finalize-clear-water.py
          python -m compileall -q rainward/tests
          node --test rainward/tests/*.test.mjs
      - name: Commit only declared Rainward candidate paths
        id: source
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add rainward/app.mjs rainward/aquatic.mjs rainward/aquatic.css rainward/world.mjs rainward/index.html rainward/release.json rainward/production-plan.json rainward/AAA_CHECKLIST.md rainward/CLEAR-WATER.md rainward/DEVELOPMENT-HANDOFF.md rainward/README.md rainward/CONTROLLER.md rainward/tests/aquatic.py rainward/tests/first-light.py rainward/tests/controller-menus.py rainward/tests/aquatic.test.mjs rainward/tests/reclaimed.test.mjs rainward/tests/handoff.test.mjs rainward/tools/build-production-plan.py
          if ! git diff --cached --quiet; then
            git commit -m 'Assemble Rainward Clear Water v0.13.1 with preserved saves and scoped acceptance'
            git push origin HEAD:feat/rainward-clear-water-20260913
          fi
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
  journey:
    needs: prepare
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        preset: [survival, classic, recovery]
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.sha }}
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install native WebGL browser
        run: |
          python -m pip install playwright==1.57.0 Pillow==11.3.0
          python -m playwright install --with-deps chromium
      - name: Native mission or explicitly isolated recovery suite
        env:
          RAINWARD_PRESET: ${{ matrix.preset }}
        run: |
          python -m http.server 4173 --bind 127.0.0.1 >/tmp/rainward-http.log 2>&1 &
          PID=$!
          trap 'kill $PID' EXIT
          if [ "$RAINWARD_PRESET" = recovery ]; then
            timeout 960 python -u rainward/tests/aquatic.py
          else
            timeout 960 python -u rainward/tests/natatorium-journey.py
          fi
      - name: Preserve exact source, failures, video and snapshots
        if: always()
        run: |
          mkdir -p test-output
          python rainward/tests/release.py
          git rev-parse HEAD > test-output/tested-commit.txt
          git diff > test-output/uncommitted.diff
          tar -czf test-output/rainward-source.tgz rainward index.html projects.css
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: rainward-clear-water-${{ matrix.preset }}
          path: test-output/
          retention-days: 14
