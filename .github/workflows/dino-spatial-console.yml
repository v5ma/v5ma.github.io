# This branch was merged in PR210 as 2a527853cffebc837f078dafdc015124b629992d.
# Retirement applies ONLY to the obsolete candidate branch. Do not merge this
# branch-local workflow replacement into master. The complete master workflow
# and its separate public/release gates are unchanged and remain authoritative.
# Exact candidate source and evidence remain at 7ae957484ac495cca8f0ce8298924d93b90e25f9.
name: Dino Atlas Field Rotunda
on:
  push:
    branches: ['dino-atlas/spatial-refinement-20260920']
    paths: ['.github/workflows/dino-spatial-console.yml']
permissions:
  contents: read
concurrency:
  group: dino-spatial-${{ github.ref }}
  cancel-in-progress: true
jobs:
  superseded:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: echo 'Candidate merged; use master run 35557266771. This bookkeeping job is not a test or acceptance result.'
