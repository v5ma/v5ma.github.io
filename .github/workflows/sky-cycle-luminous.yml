name: Sky Cycle Luminous
on:
  push:
    branches: ['sky-cycle-luminous-*']
    paths: ['.github/workflows/sky-cycle-luminous.yml']
permissions:
  contents: read
jobs:
  source:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - run: |
          mkdir -p /tmp/sky-source
          git rev-parse HEAD > /tmp/sky-source/commit.txt
          tar -czf /tmp/sky-source/source.tar.gz mario-maker-clone/svgn-paper-route tests
      - uses: actions/upload-artifact@v4
        with:
          name: sky-luminous-source
          path: /tmp/sky-source/
          retention-days: 7
