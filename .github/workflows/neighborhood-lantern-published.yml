name: Verify published unified Neighborhood Missions
on:
  push:
    branches: [master]
    paths: ['svgn-planet/**', 'level-design-library/applied/NEIGHBORHOOD-MISSIONS-WORKING-QUAY.md', 'level-design-library/applied/NEIGHBORHOOD-MISSIONS-LIVING-PORTAL.md', 'level-design-library/applied/NEIGHBORHOOD-MISSIONS-NIGHT-WATCH.md', 'level-design-library/applied/NEIGHBORHOOD-MISSIONS-NIGHT-WATCH-CAMPAIGN.md', '.github/workflows/neighborhood-lantern-published.yml']
permissions:
  contents: read
concurrency:
  group: lantern-published
  cancel-in-progress: true
jobs:
  bytes:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
          sparse-checkout: |
            svgn-planet
            level-design-library/applied
      - name: Verify exact public main game, districts, legacy layout and evidence bytes
        env:
          COMMIT: ${{ github.sha }}
        run: |
          mkdir -p published-results
          python - <<'PY'
          import concurrent.futures,hashlib,json,os,pathlib,time,urllib.request
          root=pathlib.Path('svgn-planet');base='https://v5ma.github.io/';commit=os.environ['COMMIT']
          paths=sorted(p for p in root.rglob('*') if p.is_file() and not any(x.startswith('.') or x in ('tests','tools','node_modules','__pycache__') for x in p.relative_to(root).parts) and p.suffix not in ('.py','.pyc'))
          paths.extend(pathlib.Path('level-design-library/applied')/name for name in ['NEIGHBORHOOD-MISSIONS-WORKING-QUAY.md','NEIGHBORHOOD-MISSIONS-LIVING-PORTAL.md','NEIGHBORHOOD-MISSIONS-NIGHT-WATCH.md','NEIGHBORHOOD-MISSIONS-NIGHT-WATCH-CAMPAIGN.md'])
          expected={p.as_posix():hashlib.sha256(p.read_bytes()).hexdigest() for p in paths};rows={}
          def check(path):
              row={'path':path,'expected':expected[path],'matches':False}
              try:
                  req=urllib.request.Request(base+path+'?source='+commit+'&nocache='+str(time.time_ns()),headers={'Cache-Control':'no-cache'})
                  with urllib.request.urlopen(req,timeout=25) as response:
                      assert response.status==200
                      data=response.read()
                  row.update(sha256=hashlib.sha256(data).hexdigest(),bytes=len(data));row['matches']=row['sha256']==row['expected']
              except Exception as e:row['error']=str(e)
              return row
          deadline=time.monotonic()+1500
          while time.monotonic()<deadline:
              pending=[p for p in expected if not rows.get(p,{}).get('matches')]
              if not pending:break
              with concurrent.futures.ThreadPoolExecutor(max_workers=6) as pool:
                  for r in pool.map(check,pending):rows[r['path']]=r
              count=sum(r['matches'] for r in rows.values());print('Verified',count,'of',len(paths),flush=True)
              if count==len(paths):break
              time.sleep(10)
          version=json.loads((root/'release.json').read_text())['version']
          receipt={'commit':commit,'version':version,'scope':'Exact main game, district, legacy and applied design file closure; unrelated sibling files may advance.','files':[rows[k] for k in sorted(rows)],'success':len(rows)==len(paths) and all(r['matches'] for r in rows.values())}
          pathlib.Path('published-results/bytes.json').write_text(json.dumps(receipt,indent=2))
          assert receipt['success'],'Public files did not match this exact game snapshot within the verification window'
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: lantern-public-bytes
          path: published-results/
          retention-days: 30
  live:
    needs: bytes
    runs-on: ubuntu-latest
    timeout-minutes: 50
    strategy:
      fail-fast: false
      matrix:
        route: [main, main-nav, campaign, street, xr, market, city, portal, portal-render, watch-roof, watch-combat, xr-ui, console-first-person-vr, console-third-person-vr, console-diorama-first-vr, console-diorama-third-vr, console-first-person-ar, console-third-person-ar, console-diorama-first-ar, console-diorama-third-ar]
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
          sparse-checkout: |
            svgn-planet
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install browser
        run: |
          python -m pip install playwright==1.55.0 pillow
          python -m playwright install --with-deps chromium
      - name: Verify live main entry, native district travel, campaign and spatial XR
        env:
          CONSOLE_BASE: https://v5ma.github.io/svgn-planet/
          CONSOLE_OUT: published-results/console
          MAIN_BASE: https://v5ma.github.io/svgn-planet/
          WARD_BASE: https://v5ma.github.io/svgn-planet/lantern-ward.html
          WARD_ROUTE: ${{ matrix.route }}
          CITY_SUITE: ${{ matrix.route }}
          WATCH_SUITE: ${{ matrix.route }}
          MAIN_OUT: published-results/main
          MAIN_NAV_OUT: published-results/main-nav
          WATCH_OUT: published-results/night-watch
          CITY_OUT: published-results/city
          PORTAL_OUT: published-results/portal-render
          WARD_OUT: published-results
          MARKET_OUT: published-results/market
          CAMPAIGN_OUT: published-results/campaign
        run: |
          case "$WARD_ROUTE" in
            console-*) export CONSOLE_MODE="${WARD_ROUTE#console-}"; python svgn-planet/console-browser.py ;;
            main) python svgn-planet/main-browser.py ;;
            main-nav) python svgn-planet/main-navigation-browser.py ;;
            campaign) python svgn-planet/lantern/campaign-browser.py ;;
            watch-roof|watch-combat|xr-ui) python svgn-planet/lantern/watch-browser.py ;;
            city|portal) python svgn-planet/lantern/city-browser.py ;;
            portal-render) python svgn-planet/lantern/portal-render-browser.py ;;
            market) python svgn-planet/lantern/market-browser.py ;;
            *) python svgn-planet/lantern/browser.py ;;
          esac
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: lantern-live-${{ matrix.route }}
          path: published-results/
          retention-days: 30
