name: Rainward Grounded candidate
on:
  push:
    branches: [feat/rainward-grounded-20260914]
    paths: ['rainward/**','.github/workflows/rainward-grounded-prepare.yml']
permissions:
  contents: write
concurrency:
  group: rainward-grounded-candidate
  cancel-in-progress: false
jobs:
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    outputs:
      sha: ${{ steps.source.outputs.sha }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Integrate only scoped motion and release paths
        run: |
          mkdir -p test-output
          python rainward/tools/integrate-grounded.py
          node --test rainward/tests/*.test.mjs | tee test-output/model-tests.txt
          python -m compileall -q rainward/tests
      - name: Commit the enumerated integration paths
        id: source
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add rainward/actors.mjs rainward/rainworn-humans.mjs rainward/scene.mjs rainward/world.mjs rainward/index.html rainward/release.json rainward/production-plan.json rainward/AAA_CHECKLIST.md rainward/GROUNDED.md rainward/README.md rainward/DEVELOPMENT-HANDOFF.md rainward/CONTROLLER.md rainward/tests/rainworn.test.mjs rainward/tests/aquatic.test.mjs rainward/tests/reclaimed.test.mjs rainward/tests/handoff.test.mjs
          if ! git diff --cached --quiet; then
            git commit -m 'Integrate Grounded v0.14.0 motion, coherent proportions and canonical roadmap'
            git push origin HEAD:feat/rainward-grounded-20260914
          fi
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: rainward-grounded-model
          path: test-output/
          retention-days: 14
  motion:
    needs: prepare
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.sha }}
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Load immutable prior rig for matched comparisons
        run: git fetch --depth=1 origin b87aeacb71d00b73992945daaba2fccdc66ab409
      - name: Install native browser
        run: |
          python -m pip install playwright==1.57.0 Pillow==11.3.0
          python -m playwright install --with-deps chromium
      - name: Measure and render the actual fitted characters
        run: |
          python -m http.server 4173 --bind 127.0.0.1 >/tmp/rainward-http.log 2>&1 &
          PID=$!
          trap 'kill $PID' EXIT
          timeout 650 python -u rainward/tests/grounded.py
      - name: Retain exact source and failed evidence
        if: always()
        run: |
          rm -rf rainward/tests/grounded-baseline
          mkdir -p test-output
          python rainward/tests/release.py
          git rev-parse HEAD > test-output/tested-commit.txt
          git diff > test-output/uncommitted.diff
          tar -czf test-output/rainward-source.tgz rainward index.html projects.css
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: rainward-grounded-motion
          path: test-output/
          retention-days: 14
